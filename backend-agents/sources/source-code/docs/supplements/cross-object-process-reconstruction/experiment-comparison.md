# 小实验结论与历史实验对照

当前设计已采用[事实推理、写作、最后核对](business-reasoning-and-writing.md)。用户接受采购新正文的可读性；仍有两个明确事实错误，检查对象和处理顺序已据此调整。以下记录保留实验原貌，不能改写为新设计已经全部通过。

## 2026-09-16最小研究的事实

- [最新实验报告](../../../.workspace/automatic-material-three-stage-spike-20260916/report.md)与[实际正文](../../../.workspace/automatic-material-three-stage-spike-20260916/business-process.md)保存完整。
- 全局选材从326导航、686文件目录及README自行提出系统类型和业务方向，没有固定行业答案。
- 自动包最终为32Activity、80文件，仍不够聚焦；没有送进后续DRAFT。不能据此宣称自动选材到文章的端到端通过。
- 独立三次请求使用相同人工原始包：DRAFT→规则核对→WRITE。正文可读性已接受；最后写作把已审核误写为未审核，并把支付订金/本次付款混成应付金额。
- 用户据此接受新目标DRAFT→WRITE→最终RULE_REVIEW，不要求再次验证架构可行性。新的三例验收只核实际输出，还不能记为已通过。
- 五次模型累计18分34秒，后三次13分31秒；此前四次约13分23秒。各一次不同请求不能证明减少调用会更快。
- 原先研究报告中的未通过，是当时包含准确性要求的判断；用户后来对可读性的接受应并列记录，不删掉真实错误，也不替用户判文章不可用。

## 旧跨候选有提示/无提示实验记录（历史）

以下结果属于旧两轮重建，保留比较用途；本轮三例和全仓边界由[验收](acceptance.md)规定。


状态：真实实验进行中，未完成全仓交付。此处区分实际选材、已审样本和最终发布，不把候选名称当成已识别的完整业务过程。最新运行与验证见[交付记录](delivery.md)。

## 输入独立性

两轮均从同一份原始旧目录、326条已审Activity及冻结源码开始。A的关注问题只描述需求、订单、实际收发、退回与两仓调拨，不提供Activity ID、文件路径或正确步骤。B没有关注问题，不继承A的候选、阅读请求、源码包或回答。

实际执行版本差别必须说明：B保留与成功样本匹配的原CHECK导航，A采用另获批准的CHECK-only重复导航精简。两轮全局选材和最终完整过程输入的合同不变；但不能把后续全部结果差异都归因于有无关注问题，也不为制造一致性重复调用B。

| 对照 | A：有提示小样 | B：无提示全仓 |
| --- | --- | --- |
| 成功全局选材批次 | be508810… | 711d8936… |
| 最终选材候选数 | 27：保留原24、模型新增3 | 27：保留原24、模型新增3 |
| 模型自行新增 | 采购订单、实际收发与结算联动；订单相关退回与原单衔接；两仓调拨登记与核对 | 库存单据与财务结算联动；商品主数据与库存成本维护联动；租户开通、用户配置与权限关联 |
| 阅读与重建范围 | 3/3实际候选完成阅读检查、补读、DRAFT/REVIEW并保存 | 全27候选阅读检查结束；有限修正导入14个完整匹配pair后，新独立B8cf7bb74…显式复用并处理其余13候选 |
| 最终正式五文件 | 不发布伪全仓结果 | 等待全量完成及归并 |

## 已核实的材料变化

旧方式在过程DRAFT中提供完整Activity和少量来源预览，选中的完整源码到REVIEW才进入。本轮先选择、取回、检查并执行一次补读，再把同一个完整阅读包交给DRAFT和REVIEW。

B已审样本的实际包：

- 库存与结算：16条完整Activity、1165条可定位陈述、25个来源片段，含完整DepotHeadService、DepotItemService、AccountHeadService及相关Mapper XML。
- 租户、用户与权限：12条完整Activity、769条陈述、33个来源片段，含完整UserService及租户、角色、业务关联处理、Mapper XML和页面。

新增原文让模型能够写出具体默认值、审核/反审核状态、超量配置、分批数量回写、用户ID接续及页面读取与提交保存的区别。有限核查这五类判断未发现重大语义错误；不据此承诺所有关系成立。

租户原REVIEW把一条陈述编号误填进业务用法编号，原批次仍FAILED。用户明确授权后，仅这一值在派生副本中修正；原DRAFT、原REVIEW和旧状态保留，新私有批次4bbbac…有不可覆盖清单。导入及库存样本重开均为零模型调用。它不是再次模型审阅，也不是通用自动修复。后续正常显式复用沿既有来源批次链回到该清单。

## 容量与时间的真实边界

A首读包保留19、14、13条完整Activity及各自5份整文件。CHECK仅省略不用的statementDirectory、已完整提供Activity的重复导航卡及未读卡的terms；全仓召回仍覆盖326条。实际对照确认最终过程输入、源码和Schema不变。输入加Schema编码估计约21.2万–23.8万token，不是服务端精确计数，也不包含全部封装与输出空间。

已测CLI整体范围：A选材4分19秒，B选材4分23秒，B两个并行阅读检查1分59秒；B两个完整过程样本约33分钟。这些包含读取、模型、等待和保存，不是纯RPC时间。全仓过程及归并耗时待完成后填入，不虚构加速倍数。

## 全仓首批审阅检查

B全仓批次2af66afc…已FAILED并完成排空，保存10个新候选的完整合法pair及1个租户样本复用。49次新调用均有返回：25次阅读检查、12次DRAFT、12次REVIEW；另2项阅读检查复用，全部27候选阅读阶段完成。两份最终REVIEW结构不合法，不能算已审完成；尚无归并调用或正式发布。另一个库存已审样本仍可显式复用。财务候选保留5个业务用法、6个阶段、14条规则，明确明细通过bill_id关联业务单据及欠款计算、汇总回写条件；字典与主数据维护结果应作为维护或支撑过程理解，不能因有多个阶段就算作订单履约链。

有限走读发现财务结果的一处内部不一致：审核阶段正文保留目标“1”只处理当前“0”、目标“0”只处理当前“1”，但transitions把前者称为反审核、后者称为审核。原始回答不改写，也不追加未经授权的修复调用。正式归并后必须核对是否仍保留这处矛盾；若仍存在，交付说明应列为具体语义问题，不能宣称所有状态描述已经准确。结构和引用检查通过不代表这类业务准确性通过。

商品属性候选的原REVIEW被解析拒绝。有限独立复核确认两处直接阻塞：`/processes/0/stages/1/activityUseLocalIds/0`漏了唯一匹配用法编号的末位字符`c`；`/processes/0/businessRules/8`为CONFIRMED但两类引用都为空。候选成员6/6，428个statement和10个source允许项没有未知引用，REVIEW收到完整真实DRAFT且两轮包、身份和Schema一致。用户已明确批准本段及下段共4处离线修正；派生副本修复编号并将空引用规则保守降UNRESOLVED，业务原文不改，完成结果见下文01cef52e…批次。

随后返回的操作记录候选也未保存，两处规则用法编号漏了相同的中间字符`8`：`/processes/0/businessRules/2/activityUseLocalIds/0`及`/processes/0/businessRules/5/activityUseLocalIds/2`。原过程已定义正确的“日志删除”用法；原材料、actualDraft及两轮身份一致。这两处包含在用户明确批准的4处离线修正中；不扩大到其他编号或原始模型记录。

B已完成排空，没有其他直接结构失败；独立有限复核只发现上述两份REVIEW共4处阻塞。用户明确批准后，离线批次01cef52e…FINISHED/零模型调用，保存2个派生pair、12个正常复用pair及全部27个阅读检查；两份不可覆盖清单列明4个精确差异。父协调器反向还原4字段，整份REVIEW及DRAFT与原响应一致，4个原始文件哈希不变。原B仍FAILED，副本不冒充原始模型输出。这不是自动修复或生产协议放宽；忽略驱动一次数组位置接线失败在无模型调用的旧失败批次保留，修复setter后新离线动作退出0。

新正式B批次8cf7bb74…从01cef…显式复用，预计仅余13个候选的新DRAFT/REVIEW和唯一归并。独立A的阅读7849cbb…及过程小样4efcb39d…正常FINISHED，3/3完整pair保存；A不继承B，B也不继承A。A结束后才启动B并发4，不叠加账户名额。

新B批次8cf7bb74…已FAILED并排空，14次新调用全部返回：7DRAFT、7REVIEW，保存14个复用及6个新pair，另6候选未启动。消息生命周期的REVIEW引用`support-user-session`、`support-user-login`两项未定义支撑用法，普通parser拒绝；它们在整份响应只有这两处，不能唯一映射为合法成员。实际会话及登录Activity只是阅读上下文，不能擅自新增成候选成员。有限独立复核确认其他直接结构有效：10/10成员、7阶段、11规则、10知识项，643个statement及9个source允许项无未知引用，REVIEW确实收到完整实际DRAFT。原文已经将登录定位为会话背景，并保留是否强制前置的待确认说明。最小可讨论处理是在派生副本中仅将这两项的支撑列表改为空数组，正文、步骤、规则和合法引用不动；此新修正尚未授权，不套用前4字段许可。原响应保留，无归并或正式发布。

6个新合法结果为商品主数据与库存成本、收支项目、用户业务关联、平台配置、单位配置及已有业务单据生命周期。有限正文核查看到具体依赖和更新口径，例如平台管理身份为admin、按键更新只改变配置值、单位被物料使用时拒绝删除、修改比例为空时专门清空列，以及单据状态0的编辑约束和原单分批数量回写。仍有过多日志、响应、提交成功未知等技术描述；这些具体规则不应被误报为已经自主重建了全部订单履约长链。待启动范围实际为账户、角色权限、插件、库存单据、认证会话及仓库6个候选。

## 最终仍需核对

A小样4efcb39d…已保存采购联动、两仓调拨、退回原单衔接3个完整合法pair，分别12阶段/12规则、9阶段/14规则、9阶段/11规则。真实过程DRAFT已收到完整DepotHeadService、DepotItemService、Controller及Mapper XML；采购包还含常量、关联数量汇总等补读原文。这些不是旧文档的段落，也不是人工示意。

有限正文核查能看到实际条件：关联订单号与关联请购单号不能同时填写，修改要求原状态0，分批数量驱动状态回写；调拨目标仓库必须存在且不同于源仓库，序列号/批号商品被拒绝，强审核与状态0改变保存时库存检查，明细保存后分别重算两个仓库记录。但采购结果仍未建立确定的“请购—订单—入库”顺序，保留页面选择及部分SQL口径不足；不能把候选名或12个阶段当成该长链已经完成。

正文仍有较多“材料支持／不能证明事务最终成功”等技术边界说明，影响业务可读性。先保留这一真实质量差距，不为措辞重改框架、补证据或追加模型轮次；最终交付必须区分已获得的具体业务内容与尚未达到的叙事效果。

- A的三个完整阅读、DRAFT和REVIEW是否讲清业务衔接、条件及无法确认的联系。
- B全仓详细过程及归并是否保留具体规则、可选/分批/回退路径，而非仅把入口包装成过程。
- 最终Activity、候选及已审过程的去向是否闭合；未分类和不足材料的范围是否如实显示。
- 正式业务正文及独立来源页是否可读、可查询；真实完成数、复用数及各阶段耗时。

出现某个业务名称、增加过程数量或coverage CLOSED均不单独代表上述质量通过。本轮不重新生成JDT材料、Activity或九章。
