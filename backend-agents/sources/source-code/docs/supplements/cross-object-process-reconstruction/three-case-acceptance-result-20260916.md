# 系统认识与三阶段成稿：三例实际验收结果

结论：**步骤0–6的实现与本地验证完成；步骤7真实三例验收未通过，整个计划未完成。** 不能用模型返回、保存状态或本地CI替代业务质量。下面是当前结果，不是授权重试或新的实施设计。

## 当前用户反馈与待讨论问题

用户确认：**目前看到的文档至少已经可以看懂，业务也联系起来了。** 这一可读性和业务衔接水平得到认可，不再把当前方向整体描述为不可阅读或毫无业务联系。它与下面已发现的具体准确性问题分别记录，也不改变采购缺最终核对、销售尚未形成合法完成任务的实际状态。

现阶段只记下问题，优化留待讨论，不启动此前提出的三例重做或新增模型请求：

- 采购：最终核对输入过大；长Activity/statement编号及Schema重复占用上下文。可逆短编号是待讨论方案，尚未实现。
- 销售：查询context与过程用法的输出关系未闭合，出现7个未定义编号；另有库存许可否定写反、原单进度回写适用范围扩大。
- 调拨：阶段拒绝条件和结束结果漏掉配置例外，与同稿正确规则不一致。
- 表达待办：支撑查询的呈现、通用方法规则按业务用法收窄，以及减少不必要的实现术语。用户已认可当前可读性，不为这些措辞自动扩大修改或模型调用。

全部已有材料、原稿、最终返回及日志继续保留。这里只更新阶段记录，不修改生产代码、提示词或设计合同，不将用户的可读性认可理解为批准重新生成三例或扩大到全仓。

## 1. 固定输入与执行范围

沿用原326条已审Activity、完整覆盖记录、M10、冻结Java/XML/Vue及原目录。没有运行JDT、Builder、ActivityExplainer、Step08、全仓过程重建或仓库归并。`more-findings.md`字节保持不变。

- SELECT批次：`analysis-run:6844166e1aeeab62ecb3a454c12a816779a1aa41ba366deb2296f571b5b50084`，一次真实选材成功。
- 三例批次：`analysis-run:82d416f4a16ce8b2bec7d47af8d5fa5b9a9ec00e50d3c33697b98834e40532ca`，显式复用SELECT，仅原目录序号9/23/10，已排空并为FAILED。
- 离线导出：`analysis-run:5499c1b161c05aa010f45e5d31fbde5f9136e96ebbabdd7b389191c574894f13`，显式复用已完成调拨任务，FINISHED、零新模型调用。此状态仅表示派生导出完成，不表示业务质量通过或全仓完成。
- 模型：现有ChatGPT登录、Luna/high；三个候选并行，不超过配置全局/服务并发4，无API回退。

## 2. 三例终态

| 例子 | 实际阶段 | 可用输出 | 验收结论 |
| --- | --- | --- | --- |
| 采购 | CHECK、DRAFT、WRITE返回；最后RULE_REVIEW在发送前被容量观察器阻止 | 完整阅读包、两份实际稿件、未发送的最终请求及容量记录均保留 | 未完成三阶段，不能作为已审结果复用或宣称通过 |
| 销售 | CHECK及三个过程请求全部返回 | 原始三阶段请求/返回保留；没有合法COMPLETED过程任务 | 最后结果引用7个没有定义的查询用法编号，不能合法保存；不人工清空或补造定义 |
| 调拨 | CHECK及三阶段返回，最终parser通过，完整v3任务保存 | [本次业务正文](../../../.workspace/system-assessment-three-stage-20260916/output/5499c1b161c05aa010f45e5d31fbde5f9136e96ebbabdd7b389191c574894f13/transfer/business-processes.md)、[来源页](../../../.workspace/system-assessment-three-stage-20260916/output/5499c1b161c05aa010f45e5d31fbde5f9136e96ebbabdd7b389191c574894f13/transfer/sources.md)、完整原始三稿和5项纠正记录 | 程序完成，但人工发现1类准确性问题及表达待办，业务质量未通过 |

销售最终返回中实际定义了6个办理用法；最后一个查询阶段和支撑列表却引用`debt-list-query`、`financial-bill-query`、`retail-statistics-query`、`return-link-query`、`sales-detail-query`、`sales-head-list-query`、`sales-statistics-query`。这些编号均没有定义。来源编号和statement allowlist检查没有发现越界；问题是内部用法关系，不是找不到源码。返回也混淆了查询context与办理成员：实际候选是6个member、8个context，不能为了通过保存把全部context强变成步骤。

销售原始最终响应的有限人工核查还确认两处业务错误：`processResult.processes[0].stages[3].entryConditions[2]`将库存许可写成“未被配置例外跳过，或库存足够”，否定词与同包S688:648–662相反；`businessRules[12].when`及`endResults[1]`将原单进度回写扩至销售退货、零售，而同包S688:694–707的分支不包含它们。不能仅补齐内部编号就宣称销售质量通过。这些发现来自已有原文，没有追加取证或模型请求。

采购实际最终请求加Prompt/Schema估算为252524/264448 tokens（o200k/cl100k），加原32000输出估算余量超过本次观察窗口258400。纯只读统计定位到1048个长statement句柄在索引/Schema重复；14个长Activity ID在请求/Schema共出现2595次。尚未实施短编号、改Schema、删稿件、降低窗口检查或追加调用。估算不是费用预算或服务端精确计数；适用详细设计§7的WRITE后容量失败处理。

调拨的两处明确准确性问题均在本次完整来源包内可核查：

1. `review.processResult.processes[0].stages[1].rejectionConditions[4]`省略了“强审核开启且未审核时跳过保存阶段库存检查”例外；同稿业务规则却保留了这个例外。
2. `review.processResult.processes[0].endResults[1]`把审核后刷新当前库存写成无条件；实际代码以及同稿其它字段都限定强审核开启。

调拨正文还偏实现说明，查询虽明确写为支撑，却仍放在有序阶段6；通用入库序列号拒绝分支不适合放进正常调拨删除条件。表达待办与明确事实错误分开记录，不因来源链接或未证明数据库提交而无限扩大取证。

## 3. 实际调用和耗时

| 阶段 | 真正发出的请求数 | 采购 | 销售 | 调拨 |
| --- | ---: | ---: | ---: | ---: |
| 全局选材 | 1 | 210.927秒，共用同一结果 | 同左 | 同左 |
| 阅读检查 | 3 | 147.311秒 | 180.547秒 | 136.421秒 |
| DRAFT | 3 | 358.486秒 | 418.436秒 | 266.455秒 |
| WRITE | 3 | 341.981秒 | 397.881秒 | 216.846秒 |
| 最后RULE_REVIEW | 2 | 未发送 | 825.946秒 | 325.608秒 |
| 总计 | **12** | 仅2个过程请求 | 完整3个过程请求 | 完整3个过程请求 |

正常计划无失败为13次，本次采购最后一次未发送，实际12次全部返回；不把发送前观察计成模型调用。耗时为观察器包围Provider调用的墙钟，包括子进程启动/等待/返回，不是服务端推理时间；候选之间并行，不能把表中时长简单相加当批次墙钟。离线导出复用了SELECT、调拨CHECK及完整三阶段任务，新增模型请求0。

## 4. 程序验证与交付边界

- 最新完整干净本地CI：596项、0失败、0错误、2跳过，Spotless/SpotBugs/PMD通过，退出0，9分25秒。日志在本轮工作区`inspection/local-ci-initial-preview.log`。
- 独立集成代码审查接受本计划接线；该结论不代表真实业务质量通过。
- 实际三例驱动使用独立验证JAR，SHA256为`d2712935abba3946c44f35acda558eb551bfe7e7c52d1cbbe20e47fa07b7337a`；CI后没有修改生产代码或Prompt。
- 仅完整调拨任务由既有typed preview/renderer重开导出，正文不改写、无HTML折叠；未安装正式全仓publication或伪造coverage。Markdown SHA256为`f1289407df1e8dc00b64bf2dc7cdcf23429d4dc4640b4d41e6c0c8b75f781b1c`。
- 设计保存提交`0784a33`已推送专用分支，实现尚未提交或合入main。按本计划需要三例核验通过才进行实现PR；GitHub私库连接当前也返回404，不绕过凭据读取限制。

## 5. 仍需要决定的最小事项

1. 采购：是否批准采用可逆任务局部短编号，并按新的输入合同重新完整执行该候选，而不是续接半轮。只读容量估算表明可减少大量机械ID开销，但合同/还原回归尚未实施；不重新取JDT或Activity。
2. 销售：如何在最终输出中准确表达仅供理解的查询context，避免引用不存在的办理用法；同时核对许可条件的否定词及规则适用单据范围。不能用静默清空列表通过程序校验。
3. 调拨：如何修正已定位的两个公开字段错误，确保正文、条件、规则和结束结果同步保留配置例外；技术明细表达另列质量待办。最终核对之后不擅自追加润色或第四次修稿。

这些事项尚未形成新设计批准或替换候选授权。当前可确定的是失败原因和已保存结果，不是修正后必然通过。下一步讨论限于上述具体问题，不重做分析架构、不继续全仓、不增加证据模块。
