package org.sourceanalysis.app.artifact;

/** Whether an install wrote a new publication or verified an equivalent existing publication. */
public enum ModuleInstallDisposition {
  INSTALLED,
  ALREADY_INSTALLED
}
