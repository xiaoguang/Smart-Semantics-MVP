package org.sourceanalysis.app.analysis.graph;

/** Whether an evidence link supports a program node or a program edge. */
public enum EvidenceEdgeKind {
  SUPPORTS_PROGRAM_NODE,
  SUPPORTS_PROGRAM_EDGE
}
