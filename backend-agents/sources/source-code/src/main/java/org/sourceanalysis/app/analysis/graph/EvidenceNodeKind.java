package org.sourceanalysis.app.analysis.graph;

/** The two evidence records needed to explain why one existing program element is admitted. */
public enum EvidenceNodeKind {
  SOURCE_EXCERPT,
  RULE_APPLICATION
}
