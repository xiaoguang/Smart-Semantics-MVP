package org.sourceanalysis.app.analysis.inventory;

/** The closed parser disposition assigned to every regular file in a frozen source inventory. */
public enum SourceAnalysisDisposition {
  ANALYZABLE_TEXT,
  NON_ANALYZABLE_MEDIA
}
