# Progress: Stage04 sixth-audit CandidateStore bound

- Status: COMPLETE
- Agent role: Stage04 production implementation / root-cause review
- Model: gpt-5.6-terra / xhigh
- Started: 2026-08-30
- Last updated: 2026-08-30
- Scope: CandidateStore addressed-destination bound is complete. This reopened, independent second slice is limited to persisted `EMPTY_SECTION` Trace self-description: exact section/profile/effective-template references must be emitted, replay-validated byte-for-byte, and resolved only from archive controls. No source/bounds/provider/lifecycle changes.
- Approved inputs: Scoped `AGENTS.md`; `progress/stage04-sixth-audit-tests.md`; current Stage04 CandidateStore and shared bounded-input production.
- Current branch/worktree: Shared dirty worktree; preserve all pre-existing source, test, design, and other-agent progress work.

## Completed

- Read scoped implementation rules and recorded the shared dirty worktree before any production modification.
- Created this owned progress file before making any code, test, or design change.
- Began read-only working-pattern inspection of `FilesystemCandidateStore` and the existing `CandidateValidationSupport` bounded file/directory helpers. No Maven run and no production change.
- Completed the read-only root-cause review. The exact unbounded path is `FilesystemCandidateStore.verifyArchive`: an already-addressed destination reaches `Files.list(directory).toList()` before the set/name/content comparison. Its normal caller preserves `CANDIDATE_SIZE_LIMIT_EXCEEDED` through `installedExactlyMatches`, so replacing only this enumeration with `readBoundedDirectory(directory, limit, ARCHIVE_MANIFEST_INVALID)` preserves the resource code while non-size mismatch remains the existing collision path.
- Received Luna's clean CandidateStore RED: `Stage04SixthAuditTest#addressedCandidateDestinationRejectsEntry257BeforeIdentityComparison` is 1 test / 1 assertion failure / 0 errors, expected `CANDIDATE_SIZE_LIMIT_EXCEEDED` but observed `CANDIDATE_IDENTITY_COLLISION`.
- Applied the exact one-method production correction: `FilesystemCandidateStore.verifyArchive` now obtains its addressed directory entries through existing `CandidateValidationSupport.readBoundedDirectory` before comparing count, types, names, and bytes. The remaining per-artifact no-follow bounded reads and `installedExactlyMatches` resource-code propagation are unchanged. Narrow verification is next.
- Closed the CandidateStore narrow selector: `Stage04SixthAuditTest#addressedCandidateDestinationRejectsEntry257BeforeIdentityComparison` is 1/1 GREEN. The destination's 257th entry fails at the shared resource gate before any identity comparison.
- Completed the directly covering CandidateStore, reopen/directory, and security regressions: 12/12 GREEN. `git diff --check` is clean. The independent `EMPTY_SECTION` audit test remains expressly out of scope and unrun by this slice.
- Reopened for Luna's clean `EMPTY_SECTION` RED: `Stage04SixthAuditTest#persistedEmptySectionTraceDeclaresExactRefsAndRejectsInference` is 1 test / 1 assertion failure / 0 errors; persisted Trace omits required `profileId` (`nine-section-reader-v1`). Root-cause inspection is next.
- Reproduced that exact clean RED locally: 1 test / 1 assertion failure / 0 errors, with the missing effective profile ref as the sole assertion.
- Completed the working-pattern review: `CandidateAssembler.fallbackTraceRefs` serializes the empty-section record but omits the profile; `CandidateValidationTrace.stage03ReplayCheck` already compares regenerated `trace.jsonl` bytes exactly. The resolver currently re-infers empty-section lineage from plan/item fields and built-in naming, rather than consuming a persisted record. The frozen field source is `Stage03Request.nineSectionProfileRef().profileId()`, already archived at `registry-bundle.json.effectiveContract.nineSectionProfile.profileId`.
- Frozen-reference decision: `profileId` has exactly one production source, `Stage03Request.nineSectionProfileRef().profileId()`. The effective-template and section references are the exact `templateKey` and `ownerSectionKey` of the same archived `NineSectionPlan` ReaderItem; the archived profile control is the matching `registry-bundle.json.effectiveContract.nineSectionProfile.profileId`. All three are regenerated by the provider-free Stage03 replay, so no resolver naming convention or current built-in registry is an authority for these values.
- Applied the minimal closure correction in two Stage04 files: the assembler now persists that exact `profileId`; the resolver passes the unique persisted trace record into the empty-section branch, compares its subtype/section/profile/template to the archived plan and frozen effective profile, and keeps zero-eligible accounting. The generic trace graph now retains the already-validated per-item persisted records. No technical-display, Gap, source, bounds, or lifecycle behavior changed.
- Closed the new narrow vertical: `mvn -Dtest=Stage04SixthAuditTest#persistedEmptySectionTraceDeclaresExactRefsAndRejectsInference test` is 1/1 GREEN. The real archive now exposes the exact effective profile ID and the test's coherent delete/substitute rewrites are rejected by both full validation and Trace resolution.
- Re-ran the exact method after recording the frozen-reference decision; it remains 1/1 GREEN.
- Closed the complete sixth-audit selector: `mvn -Dtest=Stage04SixthAuditTest test` is 2/2 GREEN, covering both the already-complete addressed-destination bound and the new EMPTY_SECTION closure.

## Current state

- Both bounded verticals are complete. Addressed destinations use the shared no-follow bounded directory reader before identity comparison.
- `EMPTY_SECTION` lineage is self-describing: its persisted section/profile/template triplet is compared exactly with the archived plan and frozen registry control; provider-free Stage03 replay regenerates the trace bytes before Candidate admission.

## Changed files

- `progress/stage04-sixth-audit-core.md`
- `src/main/java/com/linguan/codemd/stage04/FilesystemCandidateStore.java`
- `src/main/java/com/linguan/codemd/stage04/CandidateAssembler.java`
- `src/main/java/com/linguan/codemd/stage04/CandidateValidationTrace.java`

## Verification

| Command | Result | Key output |
| --- | --- | --- |
| Maven selectors | NOT RUN | Explicitly deferred while Luna prepares the clean CandidateStore RED. |
| `Stage04SixthAuditTest#addressedCandidateDestinationRejectsEntry257BeforeIdentityComparison` (Luna-owned) | RED | 1 test, 1 assertion failure, 0 errors: expected `CANDIDATE_SIZE_LIMIT_EXCEEDED`, actual `CANDIDATE_IDENTITY_COLLISION`. |
| `mvn -Dtest=Stage04SixthAuditTest#addressedCandidateDestinationRejectsEntry257BeforeIdentityComparison test` | GREEN | 1 test, 0 failures, 0 errors. |
| `mvn -Dtest=Stage04CandidateStoreTest,Stage04ReopenAndDirectoryBoundsTest,Stage04SecurityTest test` | GREEN | 12 tests, 0 failures, 0 errors. |
| `git diff --check` | GREEN | No whitespace errors. |
| `Stage04SixthAuditTest#persistedEmptySectionTraceDeclaresExactRefsAndRejectsInference` (Luna-owned) | RED | 1 test, 1 assertion failure, 0 errors: missing persisted `profileId` expected `nine-section-reader-v1`. |
| `mvn -Dtest=Stage04SixthAuditTest#persistedEmptySectionTraceDeclaresExactRefsAndRejectsInference test` | GREEN | 1 test, 0 failures, 0 errors. |
| Re-run of that exact selector | GREEN | 1 test, 0 failures, 0 errors (Surefire 23:50:17). |
| `mvn -Dtest=Stage04SixthAuditTest test` | GREEN | 2 tests, 0 failures, 0 errors. |
| `mvn -Dtest=Stage04TypedTraceTest,Stage04ValidationTraceTest,Stage04TraceReferenceTest,Stage04FifthAuditArchiveTraceTest test` | GREEN | 9 tests, 0 failures, 0 errors. |
| `git diff --check` | GREEN | No whitespace errors. |

## Decisions

- Reuse the shared bounded directory reader rather than add a second enumerator.
- Do not change non-empty technical fallback, Gap, provider, or lifecycle behavior in the EMPTY_SECTION closure slice.

## Blockers

- None.

## Exact next action

- Complete. No further action in this bounded sixth-audit slice.

## Resume checks

- Re-read this progress file and `git status --short`; preserve all shared changes.
