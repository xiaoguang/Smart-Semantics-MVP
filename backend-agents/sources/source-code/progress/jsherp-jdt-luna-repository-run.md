# Progress: jshERP JDT and Luna repository run

- Status: BLOCKED_FOR_REVIEW
- Agent role: run coordinator and evidence auditor
- Model: gpt-6-astra
- Started: 2026-09-13
- Scope: latest user instruction narrows this run to four real JDT entry packages → Luna/high activities/process/report → one reviewed nine-section sample document, then stop for joint review. Full frozen source remains navigation context; no full-repository model expansion, parser redesign, or frontend changes.
- Approved input: complete local jshERP commit `8c30ce7861570458920175e200bb2a6442713580`.
- Current worktree: `/private/tmp/linguan-source-analysis-process-design`, branch `codex/jsherp-jdt-luna-repository-run`, clean baseline `b5376880f59d602e9ddab5c0947512d3327e0ff2`.
- Ignored execution workspace: `.workspace/jsherp-jdt-luna-run.5Oqj9Y/`.
- Runtime checkpoint: preview exec `19188` ended exit2 after 693.413s with `BUSINESS_REPORT_PROVIDER_FAILED_AFTER_START`. Activity8, process2, repository-summary2 and reportDRAFT1 records are COMPLETED; reportREVIEW remains STARTED. No automatic retry. Exact completed knowledge and explicitly unreviewed draft Markdown are preserved. Zero-Provider export used the existing Renderer; root verified actual DRAFT byte equality, nine chapters, all47 text blocks retained,61 used refs resolving within457 originals, and4/326 activity coverage with322 not analyzed. Existence checks do NOT close semantic citation quality: the independent audit found Section7 formulas citing unrelated code. Deliver draft and findings for discussion, not accepted output. Source `92bdb80…` stays FAILED; new materials-only run `0f970b…` stays RUNNING with no worker. Formal CLI and reviewed final report are NOT complete; all model/scanner processes have ended.

## Completed

- Read the source-scoped instructions and approved four-step execution plan.
- Local Git object traversal reports 49,886 reachable objects, zero missing objects; the fixed tree contains 719 files and no unsupported tree modes.
- `codex login status` reports logged in using ChatGPT. This is authentication only, not proof of subprocess initialization permission.
- Host-permitted `codex doctor` reports healthy databases, authenticated session, connected WebSocket and reachable Provider endpoints. Sandbox-only doctor had misleading state/network failures; no state databases were modified or removed. Noninteractive TERM and historical thread warnings are not generation blockers.
- One source-free real Luna/high healthcheck completed successfully and returned `{"ready":true}`; output is `preflight/ready-output.json` under the run workspace. Customer-content calls subsequently began after inspecting the real packets; their current count is recorded below, not inferred from this healthcheck.
- Fixed tree counts: 719 files, including 273 Java and 65 XML files. The one application POM declares Java 1.8; source parsing remains under the existing configured JDT runtime, not a customer build.
- Confirmed existing JDT technical executor, business modules, source stores, and Provider adapter. Current standalone CLI needs injected bootstrap dependencies; no runnable production `main` was found.

## Current state

1. Source/runtime preflight: COMPLETE.
2. Four-entry technical and model material: COMPLETE in run `92bdb80…`; 4 BUSINESS_MATERIAL rows and 326 ENTRY_COVERAGE rows, including 322 NOT_SELECTED_FOR_SAMPLE entries. The earlier successful Step01–05 technical copy from failed Builder run `152cf03…` remains separately preserved. The reader defect is fixed; full Builder 6/0/0 and exact affected classpath fixture 1/0/0 pass. No all-entry navigation rerun is authorized.
3. Four real local activities: COMPLETE, 4/4 reviewed and saved (registration, finance, creation, batch-status), with no unexplained selected entry. Every selected entry is ANALYZED_WITH_GAPS; this is not full-scope business acceptance. Creation's two-call command took 268.160 seconds and batch-status 141.987 seconds by shell `time`; registration/finance exact per-call durations are not recorded. Business-process/repository synthesis has not started because aggregate checkpoint publication failed.
4. Four-package process/summary: COMPLETE with four singleton processes; process REVIEW removed the draft's creation-to-audit connection as unproven mandatory order. Report DRAFT: COMPLETE. Report REVIEW: failed/uncertain at Provider boundary; no final acceptance. The adapter deletes raw process diagnostics in finally and driver retained only the outer error, so exact root cause is unavailable; do not invent quota/network/Schema attribution. No parser/prompt changes or retry. Deliver explicitly unreviewed draft and preserved materials, then discuss the shortfall with user.

## Execution contract

- User explicitly authorized real Luna/high DRAFT/REVIEW tasks, then narrowed the immediate scope to four packages and a reviewed nine-section report. No repeated authorization for the same scope; no expansion beyond the latest four-package instruction.
- Before customer content is sent, retain exact frozen input, request schema, prompt identity, output location, task count and host preflight results in the ignored run directory.
- Round 1 is this new JDT-material candidate. Each internal content task permits one DRAFT and one complete REVIEW. No automatic retry or uncertain started-call replay. A replacement candidate is not launched by this plan.
- Ideal acceptance: concrete business purposes, objects, conditions, actions and results, supported cross-activity processes, full entry dispositions, and one readable nine-section report with source references.
- Fatal: wrong source, missing/invalid references or JSON, silent entry omission, missing reviewed content, invented supplied evidence, invalid chapter structure, or failed started Provider request. Preserve diagnostics; do not manufacture success.
- Quality order: inspect the four actual packets (registration, financial lookup, document creation, batch document status); execute registration first, inspect, then the remaining three packages, process synthesis and report. Completed exact input/profile requests are reused; do not silently re-call or continue to other repository entries.
- No customer build/application, source refresh, API-key fallback, second parser, old POC route, or frontend publication.
- User also requires a retained acceleration-comparison baseline: the technical copy `.workspace/comparison-baselines/jdt-four-entries-20260913.FzzCl8/` exists and its 1,396-file SHA256SUMS verifies successfully. Preserve it unchanged. After the report finishes, create a separate companion containing this successful run's materials, model requests/responses, activities, processes and report, plus input/config/code identities and timings. Never copy authentication/token/account-state data or clean either baseline/original run. Business companion remains pending report completion.
- A second technical-baseline copy is retained outside the temporary worktree at `/Users/yexiaoguang/Documents/ErpMock/linguan-prototype-v2/backend-agents/sources/source-code/.workspace/comparison-baselines/jdt-four-entries-20260913.rVawqM/technical-baseline`. The parent README explains that this is artifact backup only; development remains in the private worktree. Add the completed business companion here too, then verify both copies before delivery.
- Durable root `.../comparison-baselines/jdt-four-entries-20260913.rVawqM/` now also contains sealed `business-companion.DR55RZ/`: original `92bdb80…` materials/run, four reviewed activity files, original eight journals plus all14 current content request records, reviewed knowledge, exact completed draftJSON, explicitly UNREVIEWED-DRAFT.md, all457 source references, effective configs/policies, actual analyzer source/classes, operational scripts, runtime notes and independent citation findings. Its1459-file SHA256SUMS verification passed, approximately79MiB. No login/account/token data was copied. The companion is a preserved PARTIAL result, not a passed final report; future work creates a new directory and never overwrites either baseline.
- Time: record technical planning time separately; estimate model time only from measured first samples and actual task counts.

## Checks

| Check | Result |
| --- | --- |
| task worktree `git status --short` | clean before run |
| fixed commit object traversal with lazy fetch disabled | 0 missing objects |
| fixed tree enumeration | 719 files |
| Codex login status | ChatGPT session present |

## Active implementation and exact next action

- Luna completed `RepositoryRunMainTest`: one intended missing-launcher assertion failure, no test errors. The invalid configuration must fail before capture, run allocation, JDT, or Provider startup.
- The zero-model launcher, approved dependency handoff, saved-material sample/full continuation, and exact-request journal are implemented. The actual JDT wildcard-import runtime test passed (1 test, no failures/errors). The final offline launcher/journal/report aggregate passed 7 tests, including unknown-source rejection. Targeted Spotless and `git diff --check` passed. No parser redesign is authorized.
- Existing JDT material grouping deliberately keeps navigated entries separate. The real task count must therefore come from the actual saved materials, not an assumed four-entries-per-package ratio. No cost-based scope exclusion is authorized.
- A separate read-only audit checks the existing activity/process/report profiles and content handoffs while launcher work continues. It must not start a build, scan or model call.
- Corrected zero-model execution `analysis-run:75a8beaf1baefa893cbf6f7df66ae67f4a4516e2b2c83048850bdf1b49b77984` failed during publication. Sol/xhigh owns read-only diagnosis in `progress/jdt-index-collision-diagnosis.md`. The stack identifies global call-key deduplication, not the actual conflicting ID/fields; do not claim those before reproducing them. No blind whole-repository rerun or customer-content request follows from this failure.
- The bounded index v2 publisher/reader fix is implemented. Direct verification: index 3/0/0, graph selection 2/0/0, workflow selection 1/0/0, launcher 2/0/0. The latest expanded existing-workflow selector is **7 tests / 0 failures / 1 error**, superseding its previous one-method PASS: its reflection fixture passes null for the newly added selectedEntryIds instead of the empty default-all list. Luna is correcting only that fixture map; production null checking remains unchanged. The real launch already passes four explicit IDs and is unaffected. Re-run the affected exact method after the real scan releases the heavy-run slot. Root reviewed runtime forwarding, effective selection identity, and logging; Terra completed targeted production Spotless. No other heavy command may overlap the real scan, and no full local CI PASS is claimed.
- Four-entry execution started `2026-09-13 10:36:00 UTC`, exec session `18257`, and ended exit 2 at approximately 10:43 UTC. Step01–05 are saved, including index v2: all 4 selected contexts COLLECTED, other 322 NOT_COLLECTED. Navigation durations in selected order are 106.467 / 1.091 / 204.587 / 30.884 seconds; publication 3.448 seconds. The subsequent Builder failed at line1197 on a valid null NOT_COLLECTED context. Run is FAILED and there is no continuation state file; do not mutate it or repeat source collection. Sol is identifying existing new-execution/reuse seams; Luna writes the narrow reader/coverage RED. No customer-content Provider call has started.
- **Retained technical comparison baseline is now real**: `.workspace/comparison-baselines/jdt-four-entries-20260913.FzzCl8/` contains the exact Step01–05 run, full captured source, config/policy, log, analyzer source and tracked diff, README and a successfully verified SHA256SUMS. It is approximately 76 MiB and contains no account/login/token data. Business results will be retained in a separate companion baseline; this technical copy is not overwritten. Main index SHA is `ff53e8c4f238a53f45e5e6f4d241ad9a6cd91c3b962b85b3aa8e04ab025abf17`.
- Sol's reuse audit confirmed existing executeStep repeats the technical prefix and the saved-state modes cannot reopen a FAILED run without fabricating state. No recovery/import feature was added. After the bounded Builder fix, root independently read Builder 6/0/0 and the corrected classpath single-method 1/0/0; targeted formatting passed. A fresh four-entry-only run started `2026-09-13 10:56:35 UTC`, exec session `47947`, configuration `repository-run-four-entries-reader-corrected.json`, log `four-entry-materials-reader-corrected-run.log`, state destination `inspection/four-entry-materials-corrected-state.json`. Only state destination and declared execution scope differ from the previous four-entry config; selected source IDs and model limits are unchanged. There are still zero customer-content model calls.

| Selected sample | Exact discovered entry ID |
| --- | --- |
| UserController#registerUser | `entry:dfbb1bd121f0d3c38b75ce44152bf3a36e160454141a1a147d9cfbb07666271e` |
| AccountHeadController#getFinancialBillNoByBillId | `entry:15638e3165fbce0e7eb9f201569c2ee3fa9b5457163e6970fd7642582544c9b7` |
| DepotHeadController#addDepotHeadAndDetail | `entry:80e006395d8659aabf2034bb8a7c2fde44e68b8217a377c3ab4b331ec602a5c1` |
| DepotHeadController#batchSetStatus | `entry:06d1007987ecc379261e47e5592811763e84628536762fefd6edd77fd3d104d5` |

## Performance comparison backlog — not implemented in this run

- Shared-source syntax is already cached, but the caller repeats navigation requests for shared methods reached from different entries. A future session-local navigation cache is a candidate optimization, not a measured speedup; preserve candidate/diagnostic semantics and compare against the retained four-entry material baseline before adopting it.
- Previous execution had no completed-entry count until final publication. The selected-entry run adds ordinary per-entry start/end/count/timing logs only, without a new state machine or recovery system.
- Initial whole-run navigation lasted approximately 110 minutes and then failed in publication. This measures the failed run's elapsed time, not successful artifact generation latency, not per-entry latency, and not model latency. No claim is made about a specific repeated-query bottleneck percentage.

## First actual whole-source execution

- Started `2026-09-13 07:47:14 UTC`; direct Java process session 67959 finished with exit 0. No customer-content model calls.
- Saved run `analysis-run:a93e83b55e46bfb462e70e8496120511938df1782b53f427f614159499dc73a7`; logs and `inspection/materials-state.json` preserved under the run workspace.
- Capture/inventory covered the fixed whole source. Discovery recorded 339 HTTP sites, but admitted only 2 entries; 337 sites have `ANNOTATION_IDENTITY_UNRESOLVED`. It produced 2 materials, not accepted as complete repository material.
- Observed implementation difference: `PersistedTechnicalRunExecutor.openSession` passes an empty classpath; successful real-source JDT collection supplied existing Spring web/core/jcl 5.0.4 JARs. These exact local JARs are present and hashed. Root has not yet proven corrected whole-source results.
- Next bounded repair: test that the existing runtime carries configured approved dependencies into `VerifiedJavaProject`, then wire that input through without annotation heuristics or customer builds. Preserve this first run; use a new configuration identity for corrected material planning.
- Before large report calls, count actual schema enum occurrences and input size. OpenAI's official Structured Outputs guide documents a 1,000-enum total; the current report repeats the source-ref enum per section. This is a preflight risk to measure, not permission to omit sources or silently shrink the repository: https://developers.openai.com/api/docs/guides/structured-outputs#limitations-on-enum-size

## Corrected dependency execution

- Separate config `repository-run-with-classpath.json` and state destination `inspection/materials-with-classpath-state.json` preserve the first execution. Java 17 launches the existing JDT tool JVM 26; dependencies are the three explicitly approved local Spring 5.0.4 JARs.
- The first launch command used a relative config path and was rejected before allocation; the corrected absolute-path command ran in exec session 6984 and ended with exit 2 at approximately 09:59 UTC. This was not a model call.
- Published discovery records 326 admitted entries out of 339 sites, including `UserController#registerUser`. Thirteen unresolved sites all belong to `SystemConfigController`; they remain explicit rather than silently omitted. Navigation collection returned to its publisher, which failed at `JavaCodeIndexPublicationSpecifier.records` line 272 through `putSame`; the complete index and model materials were not saved. The exception alone does not establish per-entry navigation success or the conflicting call identity.
- Report preflight risk is concrete even on the first two-material run: 62 distinct source refs × 18 repeated enum positions + 18 fixed section enum values = 1,134. The Step08 detailed design now specifies bounded ref strings in transport Schema with the unchanged complete input allowlist and strict Java membership validation. Luna is preparing one direct large-source/unknown-ref regression; no report request has started.
- Run-local journal RED→GREEN passed the single `RunJournalStructuredProviderTest` (1 test, 0 failures/errors). Complete exact requests can be reused across sample/full execution; started/incomplete calls cannot be replayed. A separately configured provider file is prepared at `provider-run.json` for the fixed Luna/high identity, without credentials.
- Only heap-limited offline launcher/journal unit tests are permitted concurrently with the active JDT scan (`MAVEN_OPTS=-Xmx512m`, test fork `-Xmx256m`). No real JDT tests, source fixtures, quality aggregate, or second heavy verification runs concurrently. Maven ownership is explicitly handed between test and implementation agents.
- The bounded read-only audit is complete in `progress/systemconfig-annotation-audit.md`. All 13 locations and their frozen source are verified; the existing JDT log does not prove why identity resolution failed. They remain explicit coverage gaps, not invented resolved entries. No additional parser change follows from this audit.
- At approximately 35 minutes elapsed, the corrected JDT language-server process was still consuming about one CPU core; the main process remained `RUNNING` with Step01/02 publications preserved. The tool has not yet published navigation/material results. A read-only process sample showed indexing/search work, not a proven out-of-memory failure. Do not restart the scan merely because the whole-repository traversal is slower than the two-entry experiment.
- `RepositoryRunMain --mode activities-sample` and `--mode generate` now reopen the saved Step05 reference without starting capture or JDT. Provider configuration is prepared, but no customer-content task may start until the real registration and financial model packets have been inspected. The report transport schema no longer repeats the entire source-ref enum eighteen times; the complete allowlist and Java membership checks remain unchanged and the 1,001-ref regression passed.
- Whole-run scope audit: the 13 unresolved HTTP sites have no admitted `entryId`; current Step05/material/activity/report denominators contain only the 326 admitted entries. The 13 technical gaps are not automatically rendered by the current report path. The eventual delivery must include an explicit source/discovery scope statement and must not claim that all 339 HTTP sites were semantically interpreted. Detailed evidence is in `progress/repository-generation-readiness.md`.
- Navigation elapsed-time audit: the configured 30 seconds applies per JDT request; the collector also has a 30-minute soft per-entry expansion bound checked between methods. Step03 gathers all entries before publishing. Syntax and opened-document state are cached, but cross-entry navigation results are not cached by our caller. There is currently no observable completed-entry count or timeout count while collection is running. CPU/log activity cannot establish either success or percentage complete. At roughly 55 minutes, no new parsing algorithm or timeout fix was introduced; the existing scan continues. Read-only JVM attach diagnostics were unavailable and were not treated as proof of a stalled JVM.
