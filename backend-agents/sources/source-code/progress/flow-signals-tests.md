# Progress: flow-signals-tests

- Status: IN_PROGRESS
- Agent role: Luna/xhigh TDD RED test owner
- Model: gpt-5.6-luna / xhigh
- Started: 2026-09-08
- Last updated: 2026-09-08
- Scope: Maintain the bounded RED test sequence for the flow-signal work; latest slice adds the public zero-Flow→zero R0/R1/R2 task-path RED while preserving prior compiler, budget, counter, FactCandidate, Capsule, M2 publisher, zero-flow, M3, R0, and M4 evidence; no production, graph, design, fixture, schema, or Git delivery changes.
- Approved inputs: `docs/analysis-steps/05-business-flows.md` §8.1.1 and §8.6; `.superpowers/flow-signals-implementation-handoff.md`; source-code AGENTS guidance; both source-code implementation plans; baseline `2af584b59628831ab6761854056b3e1f1809e1c6`.
- Current branch/worktree: `codex/source-analysis-process-materials` / `/private/tmp/linguan-source-analysis-process-design` (root switched after docs PR11 merge; baseline `main12005e8eab122650db7783933e8acc972f6e9`)

## Completed

- Read the root, backend, and source-code instructions; the progress template; both implementation plans; the complete Step 05 design; the complete flow-signals implementation handoff; and the required TDD/verification skill guidance.
- Confirmed the worktree baseline and preserved the pre-existing root-owned change in `progress/continued-implementation-coordination.md`.
- Added exactly one test method, `emitsExactProofClosedSignalsForTwoPersistedFlowsWithoutCrossFlowBorrowing`, to the existing compiler public-seam test.
- Reopened persisted ProvenCodeFacts and ProgramGraphs public payloads and derived signal basis/closure assertions from their Fact, atom, Proof, Gap, Evidence, source-locator, and typed-boundary values.

## Current state

- The RED test uses `ProgramGraphsPublicFixture.createWithGuardedApprove(...)`, `publishProvenFacts(fixture)`, and the frozen reflective `ArtifactReference` plus seven-int compiler profile seam; its original compile-success assertion RED is retained as recorded evidence.
- Persisted premise checks now run before signal accessor lookup, including exact ApprovalClient/CancellationClient boundary keys, guard context, external-effect Gap linkage, and retained guard outcomes. The reopen helper parses only `proven-facts.json`, `proof-pack.json`, `gap-ledger.json`, `evidence-graph.json`, and `data-flow-graph.json`; it does not parse the multi-line `graph-gaps.jsonl` payload as JSON.
- Historic focused RED: the original selector failed at the absent `FlowSlice.processJoinSignals` accessor with `FLOW_COMPILATION_SHAPE_INVALID`; that mechanical failure was corrected by Terra's production signature work.
- Terra's post-format rerun of the original first-three vertical is GREEN (3 tests, 0 failures/errors/skips); the code is frozen while this counter selector runs.
- Historic counter selector result: before the list fix, the natural guarded-else fixture stopped at production line 78 while appending an entry FlowGap to the immutable upstream gap list. The bounded list fix and direct persisted guard-to-invocation test-oracle correction are now superseded by root-confirmed counter GREEN (5/5); preserve earlier failures only as historical evidence.
- Added the single counter-positive test `emitsCounterConditionOnlyForProofClosedTypedBoundaryGuardContext` and the additive `ProgramGraphsPublicFixture.createWithGuardedElseApprove(...)` natural-source variant. The test fresh-reopens persisted CFG/data-flow/evidence/Fact/Proof bytes, explicitly checks ApprovalClient/CancellationClient target keys, proves the typed FALSE boundary, exact guard-bearing FALSE edge, same-entry CLOSED guard Fact/condition Proof, both compiler outcomes, call-site-only FALSE membership, and shared enclosing basic block before the expected four-signal assertion.
- The counter test's exact basis is the union of the guard `CONTROL_CONDITION` and boundary `INVOCATION_CALL_ID`, `STATIC_TARGET_TYPE`, `STATIC_TARGET_METHOD`, `STATIC_TARGET_SIGNATURE`, and `CONTROL_CONTEXT` atoms; it requires no Gap basis and checks the persisted signal identity/closure when production emits the counter family.
- Added `returnsZeroFlowsAndGapDispositionsWhenEveryEntryExceedsTraversalBudget`, reusing the dual-entry public fixture and adding only a test-side `maxFlowNodes` override while preserving the default profile values. Its assertions require zero flows, two resource-limit GAP dispositions, retained upstream external-effect gaps, and exactly two new entry-scoped gaps with correct ownership/reason.
- Reopened this progress record for the bounded M1 publisher slice; the counter test/fixture and budget regression remain untouched.
- Prepared `persistsFreshReopenedProcessJoinSignalsAndSignalBoundFlowCompilationIdentity` using the existing unguarded dual-entry fixture and current M1 compile/publish seam. It fresh-reopens the module payload, checks v2 and both signal budgets, compares each full canonical signal array to in-memory values, verifies sorted coverage signal IDs, and recomputes the published signal-bound `flowCompilationId`.
- The released publisher selector compiled successfully and produced the intended initial RED: one test run, one failure, zero errors; the current publisher emitted `business-flows-flow-compilation-v1` where the new persistence contract expects v2.
- Applied the single authorized fixture migration for `BUSINESS_FLOWS_FLOW_COMPILATION`: `business-flows-flow-compilation-v1` → `business-flows-flow-compilation-v2`. The selector was not rerun after this policy change, per handoff.
- Reopened this progress record for the bounded FactCandidateEnumerator slice; the publisher, counter, and budget tests remain untouched.
- Added exactly one public-seam test, `enumeratesGuardCandidateWhenFalseBranchTargetsCallGraphNodeInSameEntry`, using the existing guarded-else fixture and fresh persisted-input reopen. Its setup proves the FALSE branch target is a same-entry `CALL_SITE` in the CALL graph and absent from CONTROL_FLOW before requiring the guard candidate.
- The released exact selector reached the intended assertion RED after all persisted premises passed: missing `JAVA_GUARD_CONDITION` candidate, with no setup/compiler error.
- Reopened this progress record for the bounded M2 Capsule signal-projection slice; the publisher, compiler, counter, budget, and FactCandidate tests remain untouched.
- Added exactly one projector test, `projectsFullProcessJoinSignalsWithSameFlowBasisAndIdentityBinding`, using the existing early-return guarded-approve fixture. Its setup freshly reopens the published M1 payload and proves two persisted Flow slices with complete nonempty 3/3 signal records before invoking M2.
- The projector selector compiled after two test-only AssertJ wildcard-capture corrections and its initial M1-v1 RED is historical; root confirms the complete `EvidenceCapsuleProjectorTest` is now GREEN (4/4).
- Reopened this progress record for the bounded counter-oracle correction; all production files and unrelated test fixtures remain untouched.
- Removed the four raw CFG BFS `pathContainsNode` assertions and unused `PathState` helper, which could not model compiler call-frame RETURN semantics. Strengthened persisted setup by selecting all TRUE/FALSE edges from the exact guard to the exact invocation call site and requiring one edge with FALSE kind/polarity.
- The corrected counter selector reaches the intended missing-counter RED: persisted typed boundary, same-entry CLOSED guard Fact, both outcomes, shared condition atom, and complete counter basis all pass; approve emits 3 signals instead of expected 4.
- Corrected the Capsule test oracle for shared evidence: each same-locator span must contain the current signal, while each capsule span's complete support-ID list must equal all same-Flow signals whose source locators contain that span locator. Signal-basis obligation IDs are compared after sorting by semantic signal ID rather than obligation ID.
- Added exactly one publisher persistence test, `publishesFreshReopenedSignalsAndExactCapsuleBasisClosure`, using the existing early-return two-Flow fixture. It proves persisted M1 two-flow/3-signal input before publishing M2, then checks v5, canonical M2 signal bytes against reopened M1 bytes, span support IDs, exact signal-basis obligations, capsule IDs, module address, receipt-last payload, and 14 predecessor references.
- The released publisher selector produced the legitimate initial RED: 1 test run, 1 failure, 0 errors; actual `business-flows-capsule-projection-v4` versus required v5 at the descriptor assertion, after all setup/projection premises passed.
- Applied only the authorized M2 fixture policy migration `business-flows-capsule-projection-v4` → `business-flows-capsule-projection-v5` and updated the existing publisher expectation to v5. The old publisher selector was not rerun after migration.
- Corrected the M2 publisher oracle before Terra GREEN: each serialized obligation is keyed to its projection obligation and must match kind, semantic item, and exact satisfying span IDs; satisfying spans remain within the owning Capsule. Removed the tautological same-signal filter assertion. No Maven rerun was performed after fixture migration.
- Corrected the confirmed JSON helper misuse in the M2 publisher test: span support IDs now read from `spanObject.path("supportedProcessJoinSignalIds")` with an explicit array assertion. The full publisher class is GREEN (3/3).
- Reopened this progress record for the bounded zero-flow M2 Capsule slice; the M2 publisher remains GREEN 3/3 and no fixture/schema migration is in scope.
- Added exactly one public-seam test, `projectsZeroCapsulesForPersistedZeroFlowCompilationWithCompleteGapAccounting`, using the existing dual-entry fixture and a test-only profile override of `maxFlowNodes=1` while retaining every other compiler budget value. It publishes M1 v2, then freshly reopens and proves two GAP entry dispositions, zero persisted Flow slices, nonempty disposition references/reasons, retained upstream gaps plus two entry gaps, and the exact zero-flow profile before invoking M2.
- The zero-flow test's later assertions require ordinary empty Capsule/span/obligation lists, exact freshly published M1 and proof-pack identity references, and byte-for-byte unchanged M1 payload bytes after M2. These assertions are compiled and present but are intentionally unreachable until the projector accepts an empty persisted Flow list.
- Reopened this progress record for the bounded M3 public signal slice; zero-flow projector GREEN 5/5 is root-confirmed and Maven was released for this selector.
- Added exactly one public-seam test, `publishesFullProcessJoinSignalsAndClosedCapsuleEvidenceToThePublicStep`, using the existing early-return two-Flow fixture. It compiles/publishes M1 v2, projects/publishes M2 v5, freshly reopens both module payloads, and proves two nonempty 3-signal Flow/Capsule arrays are canonical-byte-equal before invoking M3.
- The later public assertions require exactly five semantic files, Flow v2/Capsule v3 schemas, complete signal arrays, exact sorted coverage signal union, entry-disposition denominator, eligibility mapping, exact embedded span/obligation values and references, receipt controls, and all four predecessor step references. Every new helper checks array/object shape and rejects duplicate IDs.
- Corrected one bounded test-only helper omission (`jsonStrings`) exposed by the first selector compilation; no runtime assertion ran in that attempt. The rerun reached the intended M3 RED after all M1/M2 setup and full-value signal premises passed.
- Recorded the M3 RED: `FLOW_PUBLICATION_SPECIFIER_FAILED` caused by `FLOW_ACCOUNTING_INVARIANT_BROKEN` in `FlowPublicationSpecifier.requireModule`, where the stale reader still requires M1 v1/M2 v4.
- Applied only the authorized fixture policy migrations `BUSINESS_FLOWS_FLOW_SLICES` v1→v2 and `BUSINESS_FLOWS_EVIDENCE_CAPSULE` v2→v3, plus the existing Capsule schema expectation v2→v3. The unchanged old M3 selector was not rerun after migration.
- Reopened this progress record for the bounded R0 reader slice; root confirms the M3 public handoff is GREEN 4/4 and Maven is released.
- Added exactly one test, `compilesFullSignalClosedR0TasksFromFreshReopenedPublicCapsules`, using `publishBusinessFlows` and a fresh public-step reopen. It proves Flow v2, Capsule v3, two eligible Capsules, three full canonical process-join signals per Flow/Capsule, nonempty complete spans/obligations, supported-signal coverage, and satisfying-span references before invoking the R0 compiler seam.
- The later task oracle requires exactly two eligible R0 tasks, exact canonical-byte equality between each task's `inputJson.capsuleView` and its owning reopened public Capsule, same-Flow full signal arrays, and exact span support. JSONL/array/object helpers validate their shapes and reject duplicate IDs; no Provider is invoked.
- The exact R0 selector produced the intended RED: `1 test, 1 failure, 0 errors`; fresh public Flow/Capsule premises passed, then `REGISTRY_PROPOSAL_TASK_COMPILER_FAILED` was caused by `FLOW_INTERPRETATION_INPUT_INVALID` in `RegistryProposalTaskCompiler.reopenPublicFlows` at the stale Flow v1/Capsule v2 descriptor gate.
- Reopened this progress record after the branch transition; root confirms R0 GREEN 4/4 and Maven is released for the bounded M4 reader slice.
- Added exactly one test, `retainsTheFullOwningPublicCapsuleInEveryR1AndR2Task`, using the existing scripted R0 provider response and registry-freeze path. It freshly reopens public Flow v2/Capsule v3, proves two Capsules with full 14-field 3-signal arrays, exact same-Flow signal bytes, span support, and obligation closure before M4.
- The later M4 oracle requires two R1 and two R2 tasks, canonical-byte equality of each task and persisted task-set `inputJson.capsuleView` to the exact owning public Capsule, full same-Flow signal support, and both shard receipts. No real Provider is invoked.
- The exact M4 selector produced the intended RED: `1 test, 1 failure, 0 errors`; all public/R0/registry prerequisites passed, then `FINITE_KEY_FLOW_TASK_COMPILER_FAILED` was caused by `FLOW_INTERPRETATION_INPUT_INVALID` in `FiniteKeyFlowTaskCompiler.reopenCapsules` because the reader still requires Capsule v2.
- Reopened this progress record for the bounded public zero-Flow→zero local-task slice; root confirms M4 GREEN 2/2 and released the Maven slot.
- Added exactly one test, `publishesZeroFlowCompilationAsZeroR0R1AndR2TasksWithExplicitEmptyDenominators`, using the real guarded two-entry fixture, an explicit maxFlowNodes=1 profile, and the minimal public `publishBusinessFlows` overload accepting that profile. It freshly reopens all five M3 semantic files and proves two GAP dispositions, zero Flow/Capsule records, retained external and entry Gap denominator, closed coverage, an empty R0 task/shard set, a zero-call scripted Provider, an empty frozen registry, and explicit empty R1/R2 shard denominators plus persisted M4 payload closure.
- The exact zero-flow selector compiled after one test-only `canonicalUtf8()` argument correction and final array/byte-preservation oracle hardening, then produced the intended first RED: `1 test, 0 failures, 1 error`; all M3 accounting and empty-payload premises passed, then `RegistryProposalTaskCompiler.reopenPublicFlows` failed at `RegistryProposalTaskCompiler.java:502` with `FLOW_INTERPRETATION_INPUT_INVALID` because its JSONL reader rejects the canonical zero-byte `evidence-capsules.jsonl`. No R0 Provider or later M4 code ran.

## Changed files

- `backend-agents/sources/source-code/progress/flow-signals-tests.md` (this file)
- `backend-agents/sources/source-code/src/test/java/org/sourceanalysis/app/analysis/flow/compiler/EntryRootedFlowCompilerTest.java`
- `backend-agents/sources/source-code/src/test/java/org/sourceanalysis/app/analysis/graph/ProgramGraphsPublicFixture.java`
- `backend-agents/sources/source-code/src/test/java/org/sourceanalysis/app/analysis/flow/compiler/FlowCompilationModulePublisherTest.java`
- `backend-agents/sources/source-code/src/test/java/org/sourceanalysis/app/analysis/flow/compiler/FlowCompilationTest.java`
- `backend-agents/sources/source-code/src/test/java/org/sourceanalysis/app/analysis/flow/capsule/EvidenceCapsuleProjectorTest.java`
- `backend-agents/sources/source-code/src/test/java/org/sourceanalysis/app/analysis/flow/capsule/CapsuleProjectionModulePublisherTest.java`
- `backend-agents/sources/source-code/src/test/java/org/sourceanalysis/app/analysis/flow/publish/BusinessFlowsPublicationSpecifierTest.java`
- `backend-agents/sources/source-code/src/test/java/org/sourceanalysis/app/analysis/interpretation/proposal/RegistryProposalTaskCompilerTest.java`
- `backend-agents/sources/source-code/src/test/java/org/sourceanalysis/app/analysis/interpretation/model/FiniteKeyFlowTaskCompilerTest.java`
- `backend-agents/sources/source-code/src/test/java/org/sourceanalysis/app/analysis/fact/candidates/FactCandidateEnumeratorTest.java`

## Verification

| Command | Result | Key output |
| --- | --- | --- |
| `git status --short --branch` | PASS | Correct branch/worktree; only pre-existing root-owned coordination progress was modified before this record. |
| `mvn -o -t .mvn/toolchains.xml -Dtest=EntryRootedFlowCompilerTest#emitsExactProofClosedSignalsForTwoPersistedFlowsWithoutCrossFlowBorrowing test` | TEST-LOCAL COMPILE ERROR (corrected) | Main/test compilation reached two lambda-capture errors in the new guard helper; no production errors. |
| `mvn -o -t .mvn/toolchains.xml -Dtest=EntryRootedFlowCompilerTest#emitsExactProofClosedSignalsForTwoPersistedFlowsWithoutCrossFlowBorrowing test` | EXPECTED RED | JDK 17/toolchain and test compilation passed; 1 test run, 1 failure, 0 errors. Failure is `FLOW_COMPILATION_SHAPE_INVALID` caused by `NoSuchMethodException: FlowCompilation$FlowSlice.processJoinSignals()` at the reflective accessor helper. |
| `mvn -o -t .mvn/toolchains.xml -Dtest=EntryRootedFlowCompilerTest#emitsExactProofClosedSignalsForTwoPersistedFlowsWithoutCrossFlowBorrowing test` | EXPECTED RED after premise correction | Persisted reopen, exact boundary-key, Gap-linkage, guard-context, and outcome premises passed before signal lookup; 1 test run, 1 failure, 0 errors. Failure remains `FLOW_COMPILATION_SHAPE_INVALID` caused by `NoSuchMethodException: FlowCompilation$FlowSlice.processJoinSignals()`. |
| `git diff --check -- <task test and progress paths>` | PASS | No whitespace errors in the task-owned test/progress changes. |
| `git diff --check -- <counter test and fixture paths>` | PASS | Counter helper/fixture additions have no whitespace errors. |
| Maven after mechanical constructor cutover | NOT RUN BY INSTRUCTION | Terra's production signatures are transiently mismatched; root/Terra owns the next focused selector run. |
| Counter selector before bounded list fix | NOT RUN BY INSTRUCTION | Historical runtime slot was reserved by root/Terra; the later exact run is recorded below. |
| `mvn -o -t .mvn/toolchains.xml -Dtest=EntryRootedFlowCompilerTest#emitsCounterConditionOnlyForProofClosedTypedBoundaryGuardContext test` | PREREQUISITE FAILURE | Test compilation passed; 1 test ran, 0 errors, 1 failure. Compilation failed before persisted-premise reopen with `UnsupportedOperationException` at production `EntryRootedFlowCompiler.java:78` while appending an entry FlowGap to the immutable upstream gap list. |
| `mvn -o -t .mvn/toolchains.xml -Dtest=EntryRootedFlowCompilerTest#returnsZeroFlowsAndGapDispositionsWhenEveryEntryExceedsTraversalBudget test` | REGRESSION REPRODUCED | Test compilation passed; 1 test ran, 0 errors, 1 failure. The deliberate traversal-budget exit reaches the same `UnsupportedOperationException` at production `EntryRootedFlowCompiler.java:78`, before the new GAP assertions. |
| Counter selector after bounded list fix | PREREQUISITE FAILURE | Root-reported rerun reaches 4/4 compilation assertions, but only cancel compiles; approve is a Flow Gap, so the counter test fails at its expected two-Flow precondition. |
| M1 publisher persistence selector | NOT RUN BY INSTRUCTION | Root/Sol own the runtime diagnostic slot; this slice is being prepared compile-ready without Maven/JVM execution. |
| `git diff --check -- FlowCompilationModulePublisherTest.java progress/flow-signals-tests.md` | PASS | Publisher test/progress edits have no whitespace errors; Maven/JVM remains gated. |
| `mvn -o -t .mvn/toolchains.xml -Dtest=FlowCompilationModulePublisherTest#persistsFreshReopenedProcessJoinSignalsAndSignalBoundFlowCompilationIdentity test` | EXPECTED RED | Test compilation passed; 1 test ran, 1 failure, 0 errors. Failure at the new v2 schema assertion: actual `business-flows-flow-compilation-v1`. |
| M1 fixture policy migration | STATIC PASS | Only `ProgramGraphsPublicFixture`'s `BUSINESS_FLOWS_FLOW_COMPILATION` registration changed to v2; no post-migration Maven rerun performed. |
| `mvn -t .mvn/toolchains.xml -o -Dtest=FactCandidateEnumeratorTest#enumeratesGuardCandidateWhenFalseBranchTargetsCallGraphNodeInSameEntry test` | EXPECTED RED | JDK 17/toolchain and test compilation passed; 1 test run, 1 failure, 0 errors. All persisted graph/entry premises passed; failure is `MISSING_JAVA_GUARD_CONDITION_CANDIDATE` at the intended assertion. |
| `git diff --check -- src/test/java/org/sourceanalysis/app/analysis/fact/candidates/FactCandidateEnumeratorTest.java progress/flow-signals-tests.md` | PASS | No whitespace errors in the bounded FactCandidate test/progress changes. |
| `mvn -t .mvn/toolchains.xml -o -Dtest=EvidenceCapsuleProjectorTest#projectsFullProcessJoinSignalsWithSameFlowBasisAndIdentityBinding test` | TEST-LOCAL COMPILE ERROR (corrected) | New test initially hit two AssertJ wildcard-capture errors; no production/test behavior was executed. |
| `mvn -t .mvn/toolchains.xml -o -Dtest=EvidenceCapsuleProjectorTest#projectsFullProcessJoinSignalsWithSameFlowBasisAndIdentityBinding test` | EXPECTED RED | JDK 17/toolchain and test compilation passed; 1 test run, 1 failure, 0 errors. Fresh M1 two-flow/3-signal premises passed; M2 stopped at `UPSTREAM_ARTIFACT_REPLAY_MISMATCH` in `EvidenceCapsuleProjector.requireCompilation` because it still requires M1 v1. |
| `git diff --check -- src/test/java/org/sourceanalysis/app/analysis/flow/capsule/EvidenceCapsuleProjectorTest.java progress/flow-signals-tests.md` | PASS | No whitespace errors in the bounded Capsule test/progress changes. |
| `git diff --check -- src/test/java/org/sourceanalysis/app/analysis/flow/compiler/EntryRootedFlowCompilerTest.java` | PASS | Counter test-oracle correction has no whitespace errors. |
| `mvn -t .mvn/toolchains.xml -o -Dtest=EntryRootedFlowCompilerTest#emitsCounterConditionOnlyForProofClosedTypedBoundaryGuardContext test` | EXPECTED RED after oracle correction | JDK 17/toolchain and test compilation passed; 1 test run, 1 failure, 0 errors. All persisted guard/boundary/outcome/basis premises passed; failure is expected size 4 but actual 3 signals, confirming missing `COUNTER_CONDITION`. |
| `git diff --check -- src/test/java/org/sourceanalysis/app/analysis/flow/capsule/EvidenceCapsuleProjectorTest.java` | PASS | Shared-span and sorted-obligation test-oracle correction has no whitespace errors. |
| `mvn -t .mvn/toolchains.xml -o -Dtest=EvidenceCapsuleProjectorTest#projectsFullProcessJoinSignalsWithSameFlowBasisAndIdentityBinding test` | EXPECTED RED after oracle correction | JDK 17/toolchain and test compilation passed; 1 test run, 1 failure, 0 errors. Fresh M1 two-flow/3-signal premises still pass; M2 remains stopped at `UPSTREAM_ARTIFACT_REPLAY_MISMATCH` because projector requires M1 v1. |
| `mvn -t .mvn/toolchains.xml -o -Dtest=CapsuleProjectionModulePublisherTest#publishesFreshReopenedSignalsAndExactCapsuleBasisClosure test` | TEST-LOCAL COMPILE ERROR (corrected) | New test initially missed `java.util.Map`; no runtime assertion was executed. |
| `mvn -t .mvn/toolchains.xml -o -Dtest=CapsuleProjectionModulePublisherTest#publishesFreshReopenedSignalsAndExactCapsuleBasisClosure test` | EXPECTED RED | JDK 17/toolchain and test compilation passed; 1 test run, 1 failure, 0 errors. Valid two-flow/3-signal projector input passed; descriptor was actual v4 versus required v5. |
| M2 fixture policy and existing expectation migration | STATIC PASS | Only `ProgramGraphsPublicFixture` M2 registration and the existing publisher schema expectation changed from v4 to v5; no post-migration publisher rerun. |
| `git diff --check -- src/test/java/org/sourceanalysis/app/analysis/flow/capsule/CapsuleProjectionModulePublisherTest.java src/test/java/org/sourceanalysis/app/analysis/graph/ProgramGraphsPublicFixture.java progress/flow-signals-tests.md` | PASS | No whitespace errors in the M2 publisher test, authorized fixture migration, or progress updates. |
| `git diff --check -- src/test/java/org/sourceanalysis/app/analysis/flow/capsule/CapsuleProjectionModulePublisherTest.java progress/flow-signals-tests.md` | PASS | Obligation-keyed exactness correction has no whitespace errors; retained the prior pre-migration v4-v5 RED evidence. |
| `mvn -t .mvn/toolchains.xml -o -Dtest=CapsuleProjectionModulePublisherTest test` | PASS | JDK 17/toolchain; 3 tests run, 0 failures, 0 errors, 0 skipped; BUILD SUCCESS after the span JSON-helper correction. |
| `git diff --check -- src/test/java/org/sourceanalysis/app/analysis/flow/capsule/CapsuleProjectionModulePublisherTest.java progress/flow-signals-tests.md` | PASS | Final M2 publisher helper/progress changes are whitespace-clean. |
| `mvn -t .mvn/toolchains.xml -o -Dtest=EvidenceCapsuleProjectorTest#projectsZeroCapsulesForPersistedZeroFlowCompilationWithCompleteGapAccounting test` | EXPECTED RED | Test compilation passed; 1 test run, 1 failure, 0 errors. All persisted two-GAP/zero-Flow/accounting and profile premises passed; M2 fails only with `EVIDENCE_CAPSULE_PROJECTOR_FAILED` caused by `FLOW_OUTCOME_CLOSURE_BROKEN` from `EvidenceCapsuleProjector.parseFlows` on the empty Flow list. |
| `git diff --check -- src/test/java/org/sourceanalysis/app/analysis/flow/capsule/EvidenceCapsuleProjectorTest.java progress/flow-signals-tests.md` | PASS | Zero-flow test and progress changes are whitespace-clean; Maven released after the intentional RED. |
| `mvn -t .mvn/toolchains.xml -o -Dtest=BusinessFlowsPublicationSpecifierTest#publishesFullProcessJoinSignalsAndClosedCapsuleEvidenceToThePublicStep test` | TEST-LOCAL COMPILE ERROR (corrected) | The first test-only draft referenced missing `jsonStrings(JsonNode[, String])` helpers; no runtime assertion executed. |
| `mvn -t .mvn/toolchains.xml -o -Dtest=BusinessFlowsPublicationSpecifierTest#publishesFullProcessJoinSignalsAndClosedCapsuleEvidenceToThePublicStep test` | EXPECTED RED | Test compilation passed; 1 test run, 1 failure, 0 errors. Fresh M1 v2/M2 v5 two-Flow/3-signal premises and canonical span/obligation setup passed; M3 failed at the stale reader gate with `FLOW_PUBLICATION_SPECIFIER_FAILED` caused by `FLOW_ACCOUNTING_INVARIANT_BROKEN` in `FlowPublicationSpecifier.requireModule` (requires M1 v1/M2 v4). |
| `git diff --check -- src/test/java/org/sourceanalysis/app/analysis/flow/publish/BusinessFlowsPublicationSpecifierTest.java src/test/java/org/sourceanalysis/app/analysis/graph/ProgramGraphsPublicFixture.java progress/flow-signals-tests.md` | PASS | M3 test/helper, authorized v2/v3 fixture policy migration, existing v3 expectation, and progress changes are whitespace-clean; no post-migration M3 rerun performed. |
| `mvn -t .mvn/toolchains.xml -o -Dtest=RegistryProposalTaskCompilerTest#compilesFullSignalClosedR0TasksFromFreshReopenedPublicCapsules test` | EXPECTED RED | JDK 17/toolchain and test compilation passed; 1 test run, 1 failure, 0 errors. Fresh public Flow v2/Capsule v3, two-Capsule/3-signal, span/obligation/support premises passed; R0 failed at `reopenPublicFlows` with `REGISTRY_PROPOSAL_TASK_COMPILER_FAILED` caused by `FLOW_INTERPRETATION_INPUT_INVALID` because the reader still requires Flow v1/Capsule v2. |
| `git diff --check -- src/test/java/org/sourceanalysis/app/analysis/interpretation/proposal/RegistryProposalTaskCompilerTest.java progress/flow-signals-tests.md` | PASS | R0 test/helpers and progress changes are whitespace-clean; Maven released with no production/fixture/schema changes. |
| `mvn -t .mvn/toolchains.xml -o -Dtest=FiniteKeyFlowTaskCompilerTest#retainsTheFullOwningPublicCapsuleInEveryR1AndR2Task test` | TEST-LOCAL COMPILE ERROR (corrected) | The first test-only draft omitted `ReopenedAnalysisStepPublication`; no runtime assertion executed. |
| `mvn -t .mvn/toolchains.xml -o -Dtest=FiniteKeyFlowTaskCompilerTest#retainsTheFullOwningPublicCapsuleInEveryR1AndR2Task test` | EXPECTED RED | JDK 17/toolchain and test compilation passed; 1 test run, 1 failure, 0 errors. Fresh public Flow v2/Capsule v3, full 14-field signals, support/obligation closure, scripted R0, and registry premises passed; M4 failed in `reopenCapsules` with `FINITE_KEY_FLOW_TASK_COMPILER_FAILED` caused by `FLOW_INTERPRETATION_INPUT_INVALID` because the reader still requires Capsule v2. |
| `git diff --check -- src/test/java/org/sourceanalysis/app/analysis/interpretation/model/FiniteKeyFlowTaskCompilerTest.java progress/flow-signals-tests.md` | PASS | M4 test/helpers and progress changes are whitespace-clean; no production/fixture/design/schema edits. |
| `mvn -t .mvn/toolchains.xml -o -Dtest=FiniteKeyFlowTaskCompilerTest#publishesZeroFlowCompilationAsZeroR0R1AndR2TasksWithExplicitEmptyDenominators test` | TEST-LOCAL COMPILE ERROR (corrected) | Test compilation found two calls passing `VerifiedCanonicalPayload` to the local JSONL helper instead of `.canonicalUtf8()` bytes; no runtime assertion executed. |
| `mvn -t .mvn/toolchains.xml -o -Dtest=FiniteKeyFlowTaskCompilerTest#publishesZeroFlowCompilationAsZeroR0R1AndR2TasksWithExplicitEmptyDenominators test` | EXPECTED RED | JDK 17/toolchain and test compilation passed; 1 test run, 0 failures, 1 error. Fresh five-file public M3 premises, two GAP entries, retained Gap IDs, zero capsules, explicit zero-list arrays, and final same-public-step descriptor/byte preservation were reached; R0 failed at `RegistryProposalTaskCompiler.java:502` with `FLOW_INTERPRETATION_INPUT_INVALID` while reading canonical zero-byte `evidence-capsules.jsonl`. |
| `git diff --check -- src/test/java/org/sourceanalysis/app/analysis/interpretation/model/FiniteKeyFlowTaskCompilerTest.java src/test/java/org/sourceanalysis/app/analysis/interpretation/proposal/RegistryProposalTaskCompilerTest.java progress/flow-signals-tests.md` | PASS | Zero-flow test, cross-package profile overload, and progress are whitespace-clean; no production/fixture/schema changes. |

## Decisions

- Add exactly one test method named `emitsExactProofClosedSignalsForTwoPersistedFlowsWithoutCrossFlowBorrowing` to `EntryRootedFlowCompilerTest`; use only helpers in that test file as needed.
- Preserve the published approve 3 / cancel 3 expectation, null typed-boundary guard context, retained approve guard Fact and TRUE/FALSE Outcomes, and strict Fact/Proof/Evidence/Gap/source closure assertions.
- Do not add signal Java types, change production constructors, modify design/schema/POM/fixtures, or run any selector other than the exact requested Maven command.
- Applied only the frozen test-side constructor cutover: the reflective profile now accepts the `ArtifactReference` plus seven ints with appended `64, 256`; the six named profile helpers received the same appended values; the value-only `FlowCompilationTest` fixture received a final `List.of()` for signal-free signals.
- Corrected two test-only lambda captures exposed by the first exact-selector compile; the resulting RED is now at the intended missing accessor rather than test compilation.
- Corrected the test-only persisted-payload reopen boundary so setup assertions execute before the absent accessor and only single-line JSON publications are parsed as JSON; the intended missing-accessor RED remains unchanged.
- Added only the named counter test and one natural-source fixture factory/variant; its setup assertions precede the intended missing-counter signal assertion, and the pre-existing early-return 3/3 test remains unchanged.
- Added only the frozen budget regression and bounded profile override; no production or fixture changes were made for it.
- Add exactly one publisher persistence test, update only the existing v1 schema expectation to v2, and assert fresh-reopened canonical signal arrays, both signal budgets, sorted coverage signal IDs, and the published flowCompilationId signal-ID preimage.
- Keep the publisher selector serial and explicit; no runtime verification is performed in this preparation turn.
- The released publisher RED is recorded before the fixture policy migration; root/Terra owns the next GREEN implementation and rerun.
- Add exactly one FactCandidateEnumerator public-seam test for the published cross-graph guard premise; do not alter graph/compiler/fixture production behavior or widen the selector.
- The FactCandidate selector is an intentional RED at the missing guard candidate after setup, and its Maven slot is released to root/Terra.
- Add exactly one Capsule projector public-seam test for full signal copying, per-signal basis obligations/support spans, same-Flow closure, and signal-bound Capsule identity; use reflection for new M2 accessors so the first failure identifies the old M1 schema gate.
- The released Capsule selector is an intentional RED at M2's stale M1-v1 schema requirement after independent persisted two-flow/3-signal setup passed; no production or fixture migration was made in this slice.
- Remove only the invalid CFG-only path oracle from the counter test; require the direct persisted guard-to-invocation FALSE edge and retain production's private call-frame path gate as the implementation contract.
- The corrected counter selector is an intentional RED at the missing `COUNTER_CONDITION` after all setup premises pass; Maven is released to root/Terra.
- Preserve shared evidence legitimately supporting multiple same-Flow signals and exact per-span signal support; do not require a span's support list to contain only the currently iterated signal.
- The Capsule selector remains an intentional old-M1-schema RED after the oracle correction; Maven is released to root/Terra.
- Add exactly one M2 publisher persistence test and migrate only the fixture's M2 policy plus existing v4 expectation to v5 after recording the legitimate v4 RED; do not rerun the old selector after migration.
- The publisher RED is recorded with valid two-flow/3-signal setup; Terra owns the subsequent v5 implementation and verification.
- Key serialized obligations by their stable obligation ID and compare against the matching projection value; do not accept subset-only span checks or tautological signal membership assertions.
- Add exactly one zero-flow Capsule projection test using the existing dual-entry fixture and maxFlowNodes=1; prove persisted M1 accounting before M2 and assert ordinary empty M2 collections, identity binding, and M1 byte immutability after projection. Do not add M3/provider assertions or alter fixtures, schema, production, or design.
- The zero-flow selector is an intentional RED at the projector's empty-flow `FLOW_OUTCOME_CLOSURE_BROKEN` guard after all setup premises pass; later assertion shape is compile-checked and Maven is released to root/Terra.
- Add exactly one M3 public full-value signal test using fresh M1 v2/M2 v5 artifacts; compare public Flow/Capsule signals canonically, require exact local span/obligation closure and coverage union, preserve denominator/eligibility/receipt lineage, and migrate only the two released fixture policies after recording the stale-reader RED.
- The M3 selector is an intentional RED at the stale M1 v1/M2 v4 reader gate after all valid upstream premises pass; no unchanged old M3 selector was rerun after the v2/v3 fixture migration.
- Add exactly one R0 reader test over fresh public Flow v2/Capsule v3 bytes; require two eligible tasks, exact task Capsule canonical equality, same-Flow full signals and span support, and no Provider invocation. Do not alter production, fixture, schema, profile, normalization, or future M3 scope.
- The R0 selector is an intentional RED at the stale Flow v1/Capsule v2 reader descriptor gate after public prerequisites pass; Maven is released to root/Terra.
- Add exactly one M4 public-reader test over fresh Flow v2/Capsule v3 bytes; require two R1/two R2 tasks and exact canonical full-capsule preservation in in-memory and persisted M4 inputs, with same-Flow signal support and shard closure. Do not invoke a real Provider or alter production, fixture, design, schema, or profile.
- The M4 selector is an intentional RED at the stale Capsule v2 reader descriptor gate after all public/R0/registry prerequisites pass; tests and progress are frozen and Maven is released to root/Terra.
- Add exactly one public zero-Flow task-path test using maxFlowNodes=1 and a minimal explicit-profile overload; preserve canonical empty JSONL, external/entry Gap denominators, zero Provider calls, empty registry, and empty R1/R2 shard receipts. Do not add mixed eligibility/mutations or alter production, fixture, schema, design, or profiles.
- The zero-flow selector is an intentional first RED at the R0 empty-JSONL reader guard after all public accounting and empty-payload premises pass; Maven is released to root/Terra for the R0 reader cutover.

## Blockers

- Root confirms the earlier counter and projector blockers are resolved: counter GREEN 5/5 and `EvidenceCapsuleProjectorTest` GREEN 4/4. Their historical RED commands remain above for audit.
- No active blocker for the bounded signal slices: root reports counter GREEN 5/5, projector GREEN 4/4, and the M2 publisher class is now GREEN 3/3.
- The M2 publisher test oracle is now compile-ready for Terra GREEN; no further runtime verification is authorized after the fixture migration in this slice.
- No preparation blocker for the FactCandidate regression. Runtime execution is complete with the expected missing-candidate RED; no production fix is authorized in this slice.
- No preparation blocker for the counter-oracle correction. The exact selector now reaches the intended signal assertion; no production or graph-contract change is authorized here.
- The Capsule oracle correction is complete and its earlier stale-M1 schema gate is historical; root confirms projector GREEN 4/4.
- No active setup blocker for the zero-flow regression. Its exact selector reaches the expected projector RED after persisted prerequisites; production/projector handling of empty Flow lists is outside this test-only slice.
- No active setup blocker for M3. The new test compiles and reaches the intended stale-schema reader RED; production/public cutover and post-migration GREEN belong to root/Terra.
- No active setup blocker for R0. The new test compiles and reaches the intended stale public-schema reader RED; the reader cutover and later negative closure tests belong to root/Terra.
- No active setup blocker for the zero-flow test. It compiles and reaches the intended R0 canonical empty-JSONL reader RED; the reader cutover and any later M4 empty-reader behavior belong to root/Terra.

## Exact next action

- Report `COMPILE_READY` and the exact R0 empty-JSONL RED to root; tests and progress are frozen and Maven is released. No broader suite, real Provider, fixture/schema/production edit, or mixed-eligibility/mutation scope was run.

## Resume checks

- Preserve `progress/continued-implementation-coordination.md` and all unrelated worktree changes.
- Keep this slice limited to this progress file, `FiniteKeyFlowTaskCompilerTest.java`, and the minimal public profile overload in `RegistryProposalTaskCompilerTest.java`; preserve all earlier owned evidence and tests unchanged, with no production/design/fixture/schema changes.
