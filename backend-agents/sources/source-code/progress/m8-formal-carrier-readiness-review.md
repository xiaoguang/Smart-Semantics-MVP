# Progress: M8 formal carrier readiness review

- Status: COMPLETE
- Agent role: Bounded design/implementation reviewer
- Model: gpt-6-astra / ultra
- Started: 2026-09-10
- Last updated: 2026-09-10
- Scope: Read-only comparison of Step 06 M8 persisted checkpoint carriers with the formal M9 input requirements.
- Approved inputs: Scoped AGENTS.md; Step 06 sections 6.7, 7.1, 7.2, 12; M8 runners/publishers and direct tests.
- Current branch/worktree: codex/source-analysis-business-flows-closeout; /private/tmp/linguan-source-analysis-process-design

## Completed

- Read scoped instructions and M9 producer-readiness requirements.
- Confirmed existing shared worktree changes; no production, test, design, or schema writes are authorized for this review.

## Current state

Review complete. **M8 aggregate is not formal-publication ready.** It preserves a bounded execution checkpoint and per-shard counts, but cannot provide the complete records required by Step 06 sections 6.7/7.1/7.2. No P0 is asserted because this checkpoint is explicitly bounded and no M9 publication currently claims acceptance. The P1 findings below block treating it as a formal M9 input.

### P1 findings and exact consumers

| Area | Actual persisted implementation | Missing or conflicting formal requirement | Consumer impact |
| --- | --- | --- | --- |
| Task/request | `BusinessProcessInterpretationModulePublisher.java:559` writes six fields: `taskSpecId`, kind, shard, ordinal, `applicationRequest`, input SHA. `BusinessProcessInterpretationRunner.java:291` constructs P1 from only packet/kind; P2 uses `acceptedHypotheses` with two keys. | `ProcessModelTaskV1` needs schema/type, `processModelTaskId`, and full `ProcessModelRequestV1`: frozen prompt/schema/runtime values, bindings, reviewed P1 task/round and per-hypothesis projections. Model-visible request needs frozen instructions and `reviewedHypotheses`, not the early `acceptedHypotheses` field. | M9 process-model-tasks projection cannot recover task controls or the P2 review input that the Provider actually received. Section 6.7.2 requires INPUT_INCOMPLETE rather than filling these values later. |
| Receipt and round | Publisher lines 536–556 save runtime ArtifactReferences, omit configured adapter/auth and standalone schema/type. Lines 280–301 derive task/receipt/round/hypothesis/review IDs from early ad hoc content tuples. Round lines 514–529 use `taskSpecId` and a packet-key KEEP review. | `GenerationReceiptV3` requires complete expected/observed `ModelRuntimeIdentityV1` plus separate configured adapter/auth. `ProcessModelRoundV1` uses `processModelTaskId` and complete `ProcessHypothesisReviewV1`. Section 7.2 requires canonical semantic projection identities; hypothesis ID must exclude p1RoundId, whereas the current ID includes it. | M9 generation-receipt/round projection and semantic identity validation fail. Merely renaming fields or copying model/runtime names cannot repair missing configuration provenance or the old identity graph. |
| Business meaning | Runner lines 316–379 accepts exactly one hypothesis, one `TRANSITION` claim with `MAY_HAND_OFF_TO`, and KEEP; lines 449–465 require two Flow cards F01/F02 and one L01. Publisher lines 398–418 reconstruct exactly those literals instead of carrying a complete accepted semantic value. | `BusinessProcessHypothesisV2` requires memberFlows, stage/activity/role/object/state keys, purposeClaimId/endResultClaimId, full typed claims, relation bindings, and claim-bound readerSlots. Those values are not generated by the present exact response grammar. | M9 cannot construct meaningful formal hypotheses from this checkpoint. This is a missing local response/projection capability, not only a persistence omission. Do not invent purpose, process name, result, or source support in a carrier encoder. |
| Disposition and Gap ownership | Publisher lines 420–435 write a simplified disposition with empty processGaps/gapIds. Aggregate `modelSafeTerminal` copies it unchanged (ExecutionPublisher lines 227–260). `noModelTerminal` lines 263–301 copies Gap values but omits embedded taskShard and both nullable task-disposition fields, and uses reasonCode `PROCESS_MODEL_INELIGIBLE`. | Formal disposition needs schema/type/self ID, full taskShard, `p1TaskDisposition` and `p2TaskDisposition`, and all shard-owned typed Gaps. NO_MODEL reason is `NO_MODEL_SHARD`. Section 6.7.2.2 requires preservation of every safe limitation regardless of model selection. | M9/Step07 cannot audit the full shard or preserve all pending limitations. A safe successful checkpoint currently discards unselected limitations; a P1_GAP checkpoint leaves processGaps empty. |
| Formal terminal coverage | Aggregate lines 471–544 supports READY/GAP/NO_MODEL, computes one hypothesis per ready shard, and fixes failed count to zero. Runner rejects all P2 decisions except KEEP and all P1/P2 typed FAILED/P2_GAP. | Section 7.1 has NARROW/DROP/PENDING, P1/P2 typed failures, P2 GAP and potentially multiple hypotheses. | The early bounded state/count contract is valid only for its specimen. It does not demonstrate complete M8 acceptance or full process reconstruction. This is a recorded remaining capability, not a request to implement all branches in the next RED. |

Current direct tests confirm the early contract rather than the formal one: `BusinessProcessInterpretationModulePublisherTest.java:206–293` explicitly expects top-level applicationRequest, taskSpecId and fixed F01/F02/L01/KEEP. `BusinessProcessInterpretationExecutionPublisherTest.java:158–170` checks safe-shard counts and READY status, not full standalone field closure. These observations come from source inspection; no tests were executed during this review.

### Exactly one recommended next RED

After the M7 `requestMaterials` producer is GREEN, add one method to the existing aggregate public-seam test:

`BusinessProcessInterpretationExecutionPublisherTest#persistsFormalProcessTasksForP1GapFromFrozenRequestMaterial`

Use the existing real M6→M7 two-Flow/one-relation MODEL_SAFE fixture with a real limitation, a frozen nonempty prompt/schema bundle, and one scripted `P1_GAP` response. This is one behavior: **M8 materializes and persists exact formal P1/P2 tasks at their proper times while sending only the dry application request.** It avoids assuming richer business meaning already exists and keeps Provider calls exactly one.

The independent expected values are:

- Each task has exactly `schemaVersion`, `artifactType`, `processModelTaskId`, `taskKind`, `taskShardId`, `taskOrdinal`, `request`, `inputJsonSha256`.
- Each request has exactly the 12 fields in section 7.1: `schemaVersion`, `requestKind`, `taskShardId`, `taskOrdinal`, `readerApplicationRequest`, `promptBundleRef`, `responseSchemaRef`, `expectedRuntime`, `readerKeyBindings`, `reviewedP1TaskId`, `reviewedP1RoundId`, and `reviewedHypotheses`.
- P1 request copies p1Base instructions/schema, frozen prompt/runtime and exact shard packet/bindings; reviewed refs are explicitly null and both reviewed arrays are empty. The Provider receives exactly canonical `request.readerApplicationRequest`, with fields `requestKind`, `packet`, `instructions`, `reviewedHypotheses`; no wrapper/control/binding/hash fields.
- After the P1_GAP round exists, P2 task copies p2Plan instructions/schema, points to that actual P1 task and round, has empty audit/model reviewed arrays, and has `NOT_RUN_UPSTREAM_FAILED` with no P2 call/round/receipt.
- Recompute each processModelTaskId independently using the section 7.2 domain and canonical full task projection minus self ID; verify input SHA against actual dry request bytes. Fresh-reopen the aggregate and compare exact tasks without rerunning Provider.

Expected current RED: missing `processModelTaskId`/`request`, wrong application field set, missing frozen instructions, and early identity formula. It must be an assertion failure after the real producer fixture reaches M8, not missing M7 inputs or test compilation failure. Only this task-materialization behavior should be implemented before choosing the next RED.

### Design-authority follow-up, not an added test queue

The existing section 6.7.2 phrase “carry already produced semantic values” is insufficient for hypotheses: present P1 grammar produces none of the required purpose/end/reader-slot values. Before a later hypothesis RED, the design authority must freeze a bounded packet-local response representation and its exact semantic projection. Do not make Terra fill those values from method names. Likewise, receipt configuration must have an explicit frozen source; configured adapter/auth cannot be inferred from the runtime ArtifactReference. This review makes no design changes and requests no expansion of M9.

## Changed files

- progress/m8-formal-carrier-readiness-review.md

## Verification

| Command | Result | Key output |
| --- | --- | --- |
| git status --short | Observed | Shared worktree contains existing design, implementation, and test changes; preserve them. |
| Read Step 06 sections 6.7, 7.1, 7.2, 12 and direct M8 sources/tests | Complete | Source-level field and identity comparison only; no Maven or Provider invocation. |
| git diff --no-index --check /dev/null progress/m8-formal-carrier-readiness-review.md | PASS | No whitespace errors in the owned review file. |

## Decisions

- Report one smallest next RED, not a queued group of tests.
- Do not run Maven, Provider, network, or M9 implementation.

## Blockers

No blocker for the review. M8 formal publication readiness remains blocked by the P1 findings above.

## Exact next action

Root to finish M7 producer readiness, then assign Luna the single task-materialization RED above. Do not begin M9 publication from the current checkpoint.

## Resume checks

- Reopen this progress and check Git status.
- Recheck referenced file lines if parallel implementation changes them.
