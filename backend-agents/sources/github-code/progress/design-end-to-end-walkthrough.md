# Progress: design end-to-end walkthrough

- Status: COMPLETE
- Agent role: single design author
- Model: gpt-5.6-sol / ultra
- Started: 2026-08-29
- Last updated: 2026-08-29
- Scope: Apply three final minimal corrections: add the MyBatis `@Param` import dependency to A16 and update its counts, narrow P06's initial basis to assignment atoms, and distinguish nine-anchor display totality from six-policy-kind coverage.
- Approved inputs: Repository/source instructions; current `DESIGN.md`; current `README.md`; `docs/stages/00-mvp.md`; shared `NineSectionProfile`; the approved M1–M8 brief; and the read-only Sol/ultra review findings. No live source, model-provider, network, customer build, test, generated artifact, deployment, commit, or push.
- Current branch/worktree: `codex/github-code-design-walkthrough` in `/Users/yexiaoguang/Documents/ErpMock/linguan-prototype-v2`

## Completed

- Read all required repository, backend, source, design, stage, shared-contract, progress-template, and codebase-design instructions in full.
- Confirmed the branch and preserved two unrelated pre-existing modified files outside the assigned source directory.
- Classified the work as an approved architectural documentation rewrite and fixed the M1–M8 deep-module seam vocabulary.
- Replaced `DESIGN.md` with a complete vertical M1–M8 target walkthrough using one synthetic inventory-reservation repository through all eight modules.
- Added the capability-envelope theorem, recomputable AssuranceLedger, complete reader-facing nine-section result, generation governance, concise DepotHead counterexample, stable ARCH appendix mapping, and retained current maturity matrix.
- Updated `README.md` so the primary reading path is vertical flow → complete walkthrough → feasibility proof → complete nine-section result → current POC status.
- Completed the requirement-by-requirement self-review and documentation-only verification gate.
- Corrected the reviewed design to one entry-rooted FlowSlice with four nested OutcomePaths, one minimal complete-process EvidenceCapsule, and one M5 task.
- Replaced free-form factual interpretation with frozen finite BusinessTermRegistry selection, finite ClaimVocabulary admission, and deterministic ReaderSentenceTemplate rendering; isolated open label/gloss candidates as sidecar-only `HUMAN_REVIEW_REQUIRED`; preserved seven term-backed meaning lineages and five versioned Gap provenance records.
- Reconciled MyBatis result mapping, candidate/rejected atom accounting, expected-not-executed assurance, Fact/meaning Trace, upstream Capture ownership, and the explicit `improveCandidate` Reader Candidate Round 2 seam.
- Corrected every machine locator to canonical repository-relative form, made the complete-process Capsule spans non-overlapping, included the Mapper XML namespace, and closed the loaded-version Proof from SELECT result through the record accessor and Mapper parameter to the UPDATE predicate.
- Made the two-Reader-Candidate limit structural through one Round-1 slot and one append-only atomic Round-2 slot per series, with exact parent/finding lineage, idempotent replay, and conflict rejection.
- Passed the fresh documentation-only structural, semantic-consistency, link, whitespace, branch, and exact-scope checks after the review fixes.
- Closed `resultType="example.inventory.InventoryRow"` through the Service package/record, exact SELECT component mappings, bound `findBySku` return/local variable, accessor call argument, Mapper `@Param` import/parameter, and UPDATE predicate in a 16-node/23-edge ProofPack example and mutation strategy.
- Separated the complete deterministic `ProofDependencyClosure`/ProofPack from the six-span non-overlapping `modelEvidenceSpans` projection; the model sees only the latter plus normalized Facts and Proof references.
- Made display resolution total through a six-anchor `TechnicalDisplayRegistry`, retained seven fully specified frozen `BusinessTermRegistry` entries, and made M6 priority/conflict behavior explicit.
- Converted every sample AssuranceLedger coverage metric to typed expected/observed fields and kept all observed values null under `EXPECTED_DESIGN_WALKTHROUGH_NOT_EXECUTED`.
- Defined the shared Round-1/Round-2 persisted slot state machine, three-attempt prestart ceiling, same-slot repaired restart, atomic `thread.started` consumption, terminal post-start failure, and conservative crash recovery.
- Corrected P06's initial basis to A13/A14 while retaining NARROW for unsupported claim keys, and separated 9-anchor technical-display resolution totality from 6-policy-kind coverage.

## Current state

- The three minimal Sol/ultra review corrections and their targeted documentation checks are complete. No product implementation, live scanner/model run, customer build/test, network action, generated artifact, commit, or push was performed.

## Changed files

- `backend-agents/sources/github-code/DESIGN.md`
- `backend-agents/sources/github-code/README.md`
- `backend-agents/sources/github-code/progress/design-end-to-end-walkthrough.md`

## Verification

| Command | Result | Key output |
| --- | --- | --- |
| `git branch --show-current` | PASS | `codex/github-code-design-walkthrough` |
| Reader H2 + M1–M8 subsection checker | PASS | Exactly 9 required reader H2; M1–M8 each contain all 9 required module fields. |
| Flow/atom/meaning/Gap checker | PASS | 1 FlowSlice, 4 OutcomePaths, 1 Capsule, 1 model task; A01–A20 each owned once and exposed; 7 meaning lineages; 5 complete Gap provenance records/questions. |
| Typed protocol/MyBatis/Capture/Trace checker | PASS | No free-form factual response fields; P07 bounded; explicit resultType/FQN/return/local/accessor/parameter/predicate chain; upstream Capture separated; Fact and meaning Trace both present. |
| Ledger/round/binding checker | PASS | `EXPECTED_DESIGN_WALKTHROUGH_NOT_EXECUTED`; 20 candidate = 20 admitted + 0 rejected; digest matches null; typed Java-call 3/3 and Mapper-statement 2/2 expectations with null observations; identical R1/R2 evidence base; P09→G05; `improveCandidate` present. |
| Reader/internal-token + ARCH/maturity/Depot checker | PASS | Complete body has 20 atom exposures, 7 frozen-registry term-backed meanings, 5 gaps and no internal IDs/SHA/runtime fields; ARCH mapping 12, maturity rows 12, DepotHead 2 PASS/3 FAIL. |
| Fence/link/whitespace checks | PASS | DESIGN fences 64 and README fences 14 (both balanced); 22 JSON fences parse as JSON or JSONL; no Mermaid/unresolved markers; local files and README design anchors resolve; `git diff --check` clean; no trailing whitespace. |
| `git status --short` exact-scope check | PASS | Authorized source scope is exactly DESIGN, README, and this progress file; two unrelated pre-existing docs remain present and untouched by this task. |
| Build/tests/model/network | NOT RUN | Explicitly prohibited for this documentation-only design-authoring task. |
| Final review-fix semantic checker | PASS | No nonexistent basename or open-label auto-render field; all machine locators canonical; one six-span non-overlapping model Capsule; full ProofPack contains XML namespace line 4, Mapper `@Param` import line 4, and a 16-node/23-edge loaded-version chain; one Flow/one task/four Outcome; 7 complete finite term entries and 7 meaning lineages; five Gap provenance rows; exact 20 reader atom exposures; separate binding denominators. |
| ProofPack/Capsule/registry checker | PASS | Full dependencies stay in ProofPack; model payload excludes package/config/namespace/resultType/receiver nodes; six supported anchor kinds each have one nonempty technical fallback policy and all nine concrete fixture anchors have a unique resolution; every one of seven business-term entries has key, anchor kind, localized value, eligible kinds, minimum basis, integer priority and fallback reference. |
| Final three-fix checker | PASS | A16 includes canonical Mapper import node N16 and import-to-parameter edge with 16/23 ledger counts; P06 uses initial basis A13/A14 and still NARROWs unsupported claims; typed ledger reports 9/9 anchor resolution totality separately from 6/6 policy-kind coverage. |
| Candidate-series/ledger checker | PASS | Both round slots use `RESERVED`/`PRESTART_RETRYABLE`/`STARTED_CONSUMED`/`COMPLETED`/`TERMINAL_FAILED`, immutable canonical request identity and a maximum of three same-slot prestart attempts; unique Round 2 makes a third Candidate unrepresentable; rejected dispositions remain arrays; ledger metrics are typed and unexecuted observations remain null. |
| README/adapters checker | PASS | Reading order is vertical flow → M1–M8 walkthrough → feasibility/ledger → complete nine-section body → maturity; only a future local-loopback HTTP Adapter belongs to M8, while remote Git Capture is an explicit separate upstream workflow. |

## Decisions

- M1–M8 will be the primary target-design structure; ARCH-01..ARCH-12 will remain stable only in an appendix mapping.
- The same explicitly synthetic Java 17/Spring MVC/MyBatis repository will be carried through all modules and the complete reader-facing nine-section document.
- Safety, resource ceilings, and no customer execution will be stated as cross-cutting gates over every module.
- Current POC facts will remain separate from target-design claims.
- M5 deterministic admission uses a frozen finite `BusinessTermRegistry` plus a Fact-derived `AllowedClaimRegistry`; arbitrary model prose never enters automatic reader content or meaning grounding.
- Assurance is a recomputable vector of denominators and findings, never a model probability or scalar confidence score.
- Full ProofPack closure and model-minimal Evidence projection are independently validated products; neither is a substitute for the other.
- Technical display fallback is a total, frozen function over supported anchor kinds; absence of an eligible business term never authorizes open model naming.
- Round retries reuse one persisted slot and canonical request identity; only a fully proved pre-`thread.started` failure may become retryable, and a started round is consumed permanently.

## Blockers

- None.

## Exact next action

- None. The assigned documentation-only correction pass is complete.

## Resume checks

- Re-read this file, run `git status --short`, confirm the branch, and inspect any changes in `DESIGN.md`/`README.md` before continuing.
