package com.linguan.codemd.target.stage02.applicationprofile;

/** Deterministic treatment of one observed framework or configuration signal. */
public enum SignalDisposition {
    SUPPORTED,
    UNSUPPORTED,
    AMBIGUOUS,
    OVER_LIMIT
}
