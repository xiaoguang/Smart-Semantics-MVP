package com.linguan.codemd.target.stage02.applicationprofile;

/** Languages that the current Stage02 discovery profile can determine without executing a build. */
public enum ApplicationLanguage {
    JAVA
}
