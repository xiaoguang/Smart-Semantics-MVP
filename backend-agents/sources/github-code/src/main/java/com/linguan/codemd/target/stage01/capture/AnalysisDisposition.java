package com.linguan.codemd.target.stage01.capture;

/** Whether a captured regular Git blob may be supplied to a source parser. */
public enum AnalysisDisposition {
    ANALYZABLE_TEXT,
    NON_ANALYZABLE_MEDIA
}
