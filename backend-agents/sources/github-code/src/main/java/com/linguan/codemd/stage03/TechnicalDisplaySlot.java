package com.linguan.codemd.stage03;

/** A display value from a validated total technical fallback policy. */
public record TechnicalDisplaySlot(String slotKey, String anchorKey, String value) implements ReaderSlot {
}
