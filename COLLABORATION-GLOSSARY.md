# 协作领域词汇

## 组织与空间

- **产品**：最外层产品身份，本原型固定为“零售语义建模”，不需要用户反复选择。
- **协作工作空间**：成员和角色的权限边界。“ERP 数据治理”与“零售经营建模”是两个协作空间，不是两套产品。
- **模型项目**：持续演进的一套业务语义模型。当前只有“管伊佳 ERP 语义模型”和“零售经营语义模型”两个项目；草稿、申请和 Catalog 均以项目隔离。
- **证据包**：同一模型项目的一组输入资料，例如设计文档、数据库快照、源码或多存储资料。证据包不是模型，也不占用 Catalog 版本号。
- **来源版本**：Markdown v1–v4、数据库 Snapshot 或 Git Commit 等单一来源身份。
- **资料批次**：多个证据包冻结后的不可变组合，使用 R1/R2 等修订号；它不等于 Catalog V1/V2。
- **成员关系**：某个用户在某个协作工作空间中的角色与有效状态。角色不是账号的全局属性。

## 来源配置与证据

- **连接器类型**：平台支持的一类读取方式及其配置、范围和证据能力，例如 MySQL、MongoDB、Semantica RDF、对象存储或 Kafka。它不是某个具体环境的连接。
- **共享连接**：协作空间中命名并可复用的来源配置，例如“零售交易库”。模型项目只引用连接产生的快照，不直接拥有连接凭据。
- **配置版本（revision）**：共享连接的一次不可变配置修订。修改地址、读取范围或凭据引用会创建新 revision，不会改写旧 revision。
- **读取范围**：一次连接允许发现和读取的 Schema、表、目录、索引、图、Topic 或仓库路径。编辑者可以在生成快照时缩小范围。
- **证据快照**：某个配置 revision 和读取范围产生的不可变读取结果，具有版本引用、指纹、清单和证据定位。快照不是正式模型。
- **建模批次**：模型项目选择若干快照后冻结的组合；批次指纹与快照选择顺序无关。只有批次进入互证和候选建模。
- **凭据引用**：指向外部密钥的标识，例如`secret://retail/mysql-readonly`。体验环境只保存引用，不保存密码、Token和私钥。

## 模型与变更

- **标准建模文档 Artifact**：M3 交给 M4 的不可变九段 Markdown 与结构化审核信封，身份由 artifact ID、revision 和 Markdown SHA 共同确定。它不是证据审阅说明书。
- **Review Issue**：人类需要检查的差异、缺口或提醒。查询时先读问题索引，不随问题一起全量读取证据正文。
- **人工结论（Decision）**：作者为阻断问题选择允许的 resolution 并填写理由后形成的审计事实；零售三项阻断必须一次写回 r9。
- **根来源**：业务事实独立起源的来源。派生知识图或索引沿上游血缘归到根来源，不重复增加独立真相数量。
- **对象证据状态**：Catalog 中每个可见对象持久保存的`VERIFIED`或`NEEDS_CONFIRMATION`状态。前者必须有证据，后者必须没有证据，二者满足 XOR。

- **正式模型**：所有成员共同看到的当前 Catalog 数据。它始终只读。
- **Catalog**：一次成功发布形成的不可变模型版本，记录作者、审核者、发布者、证据和来源申请。
- **个人草稿**：某个用户基于正式 Catalog 创建的私有工作副本。AI 建模和另外五个业务页面的修改都写入同一份草稿。
- **变更申请**：作者提交并冻结某一草稿修订后形成的审核单，包含基线、差异、证据和完整候选数据。
- **基线**：草稿创建时所依据的 Catalog 版本及指纹。发布前若正式基线已变化，申请进入冲突状态。
- **修订**：个人草稿每次成功写入后的乐观锁版本。审核退回会恢复草稿并产生新修订，旧审核不再适用于新内容。

## 角色与流程

- **查看者**：查看正式模型和历史版本，不能创建草稿或处理申请。
- **编辑者**：创建和编辑个人草稿，提交变更申请。
- **审核者**：查看证据和人类可读差异，批准、退回或拒绝其他作者的申请。
- **发布者**：将已批准、无冲突且基线仍有效的申请发布为新 Catalog。
- **管理员**：管理两个协作工作空间并具备全部能力，但不能审核或发布自己的变更。
- **四眼原则**：作者、审核者和发布者的职责分离；至少另一位成员必须审查变更，作者永远不能发布自己的申请。

## 两类“审核”

- **候选决定**：AI 建模过程中把某个候选“加入草稿”或“不加入草稿”，只影响作者的个人草稿。
- **协作审核**：作者提交完整草稿后，由审核者决定变更申请能否进入发布；它不会直接编辑候选对象。

这两类决定使用不同状态和记录，产品文案不得把“加入草稿”写成“正式审核通过”。
